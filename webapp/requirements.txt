# webapp + 셀 데이터 모듈 의존성 (루트에 다른 requirements 가 없다 — 이 파일 하나).
#
# ⚠ 메이저 상한을 건다. 선행 webapp 이 전부 `>=` 였을 때 재배포마다 Flask/Markdown 이 조용히 올라가서
#   "어제 되던 화면이 오늘 다르다" 를 추적할 수 없었다. 상한은 **메이저만** 걸어 보안 패치는 받되 파괴적 변경은 막는다.
# gunicorn 은 넣지 않는다 — 이 앱은 127.0.0.1 로컬 열람 전용이고 배포 대상이 아니다.
Flask>=3.0,<4.0
markdown>=3.5,<4.0
PyYAML>=6.0,<7.0
pandas>=2.0,<3.0          # tools/cells (raw CSV → 사이클 요약)
anthropic>=0.40,<2.0      # /chat (키 없으면 화면만 비활성)
