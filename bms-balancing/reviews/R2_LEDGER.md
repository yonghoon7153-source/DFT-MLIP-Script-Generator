# R2 라운드 원장 — 발견 통합 · 상태 · 순서 (대상 abfed8b, 2026-09-11)

> **다음 세션은 이 파일부터 읽는다.** 리뷰 원문: `R2_CODEX.md`(외부, NO-GO), `R2_INTERNAL_L2.md`, `R2_INTERNAL_L5.md`, `R2_INTERNAL_L0.md`(본인 변이 시험). 내부 L1·L3·L4 는 세션 한도로 실행 못 함.
> Codex 반례 재생: `python3 reviews/r2_repros/harness_r2_replay.py --target <bms-balancing 절대경로> --output /tmp/r.json` → 네 케이스 rc 0 = 반례 성립 (2026-09-11 abfed8b 에서 확인).

판정: **NO-GO**(Codex). 반박 단계 결과 — 열 건 전부 우리 트리에서 재현. 반박 성립 없음. 내부 L2·L5 가 그 위에 결론급 셋(L5-F1·L2-F1·L2-F2)을 더했다.

## 통합 발견 (심각도 순)

| ID | 무엇 | 출처 | 상태 |
|---|---|---|---|
| C1 | **대조(fixedhc)는 LAM_PE 쪽에서 원통형 패턴을 재현한다.** "대체는 원인이 아니다" 는 LLI 띠에만 성립; "LAM_PE 식별 가능성 똑같다"·"대조가 파우치 패턴 유지" 는 산출물과 반대 | L5-F1 | 열림 → §1-12·INTRO·FOR_BMS 재작성 |
| C2 | **측정 NE 는 목적함수가 소비하지 않는다** (`Objective` 는 `half.E_PE`+`blend.E`). "35.77 mV 를 지웠는데도" 강도 논증 무효; 파우치 효과의 원통형 이전 가정 없음 → "대체를 배제" 철회, "파우치 pristine 고정 실험에서 큰 폭이 재현되지 않았다" 로 한정 | R2-07, L5-F2 | 열림 → §1-12·§7-3·FOR_BMS·INTRO·HANDOFF |
| C3 | **비교기가 비교하지 않은 값도 "전부 일치"** (행 누락·격자 불일치·NaN) | R2-01 | 열림 → verify._compare_dd_eval + RED |
| C4 | **짧은 정확 토큰 `1.5` 가 파일 전체 atol 0.1** 로 → 163 % 차이 은폐 | R2-02 | 열림 → printed_abs_tol 열별/전정밀도 + RED |
| C5 | **cmd_profile 이 실패한 optimizer 결과를 적합으로 저장** (A3 미종결 경로) | R2-03 | 열림 → 공통 채택 정책 + 상태 열 + RED; 기존 profile 산출은 "수렴 미확인" 표시 |
| C6 | **외곽 범위 겹침 ≠ 공유 가능값** (가능집합 비연결 가능; 산출은 증인 미보존) → "원통형은 못 가른다" 를 "외곽 범위가 겹친다" 로 | R2-05 | 열림 → §1-12·FOR_BMS 문구, 격자 도달 목록을 JSON 에 보존 |
| C7 | **/best 정규화는 교란 제거가 아니다**; best_obj 는 auto-scale 단위. 물리 단위 rmse 로 5.6x/2.9x(겹침); 8.0x 는 max/max(min 3.7·med 6.5); LAM_NE 정규화 min/min 0.88 | R2-06, L5-F4, L2-F6 | 열림 → §1-12 "임의 재척도화 기술값" + rmse 표 병기, "벗긴 값" 철회 |
| C8 | **원통형 부적합이 사이클과 함께 커진다** (24→39, 31→47 mV; 파우치 9.8→7.6) → 잡음보다 모델 부적합 쪽. U3 를 좁힐 수 있다 | L5-F4 | 열림 → §1-12 U3 문단 |
| C9 | **§5-2 (b)/(a) 는 표현력이 아니라 적합이 움직인 Δγ**; (a) 를 맞출 Δγ 는 상자 안; b_NE 만 바꾸면 LAM_NE=0; 보상은 a_PE·b_PE 로도 샘 | R2-08, L5-F3 | 열림 → §5-2 재작성, ne_shape 판정문 수정 |
| C10 | **`<1.20 이면 음수 불가` 는 틀림** (행별 임계 1.177~1.196, 고정-기준 조건); "인과가 닫혔다" 는 부호 산술까지만 | R2-09, L5-F5 | 열림 → §2-1·audit97 출력 |
| C11 | **§1-10 "100 GITT 뒤집힘은 §2-1 과 같은 기전" 은 반대 기전** (양쪽 눌림 → LAM_NE 가 1−c/c_ref 로 붕괴 4/8; LAM_PE 폭은 기준 a_PE 산포) | L5-F5 | 열림 → §1-10 문단, FOR_BMS §3-1b 경고 |
| C12 | **`best ± span/2` 표시가 구간을 옮긴다** (best 는 중점이 아님; 6 구간에서 0 포함 여부까지 틀림) | R2-04 | 열림 → compare_states `best [min,max]`, §1-10 표 형식 |
| C13 | **INTRO §6-1 "원표 안 올렸다/상관이지 인과 아님"** — §2·§2-1 과 반대; FINDINGS §0-2 행 5·§7-1 항 3 도 옛 문장 | L2-F1 | 열림 |
| C14 | **HANDOFF §4 "셀 일반화는 아직 없다"** vs §5 "끝났다" | L2-F2 | 열림 |
| C15 | **FOR_BMS_TEAM 오도 문장 5**: "답이 바뀌나 예(7~15 %)" 미측정 · PE=pristine 이 P2 "답 안 바뀜" 에(대조 실측 LAM_PE −2.65 %p) · "16/97" · "10.5 배" 무조건 · "다섯 번에 한 번" n=5 | L5-F6, L2-F5 | 열림 |
| C16 | **§0-1 요약이 본문보다 뒤처짐** (104 값·한 셀 300_0009·절차 동치 아님); R2 요청문이 §0-1 을 바꿔 인용 | L2-F7 | 열림 |
| C17 | HANDOFF 3.3985(v1) → 3.6027; "8 조합 중 6" → 5; "3분의 1" → 16~21 %; 5/8~9 는 산출물에 없음 | L2-F3, L2-F4 | 열림 |
| C18 | `ne_shape` CSV `gamma_ref` 열이 대상 γ (스키마 오류) | R2-10, L5-F3 | 열림 → 열 둘로 + 재생성(사용자 기계) |
| C19 | 철회 문장 잔존 묶음: matlab/README "실질 오차막대" 제목 · INTRO §6-3·FOR_BMS :375 "반복 측정 필요" · README:134 · FINDINGS:527/531 · WORKING_STATE "유일하게 남은 실측"/"원통형 13 행" · CODEX 요청문 "셀은 넷"/"13 행" · `1629b82`→`3fdeaa2` | L2-F8 | 열림 |
| C20 | 테스트 사각지대: §1-10 표 칸, FOR_BMS·INTRO·HANDOFF 숫자 무검사; `fixed_hc.sha` 첫 청크; `tol_percent_of_best`; 한정어 삭제; `_interp_lin_extrap` 중복 x 조용히 통과(실데이터 도달성 미확인 — L4 미실행) | L0 | 열림 |

## 닫는 순서 (예산이 끊겨도 앞의 것부터 남는다)

1. 이 원장·리뷰 원문 커밋 (`reviews/`) ← **완료 시점에 이 줄을 갱신**
2. 코드: C3·C4 (비교기) → C5 (profile) → C12 (표시) → C18 (스키마) → C10 (audit97 출력). 각각 Codex 반례를 RED 테스트로 먼저.
3. 문서 철회 패스: §0-2 에 R2 철회 표 추가; 철회 문장을 취소선+정정으로 (C1·C2·C6·C7·C9·C10·C11·C13·C14·C16·C19).
4. 문서 재작성: §1-12(C1·C2·C6·C7·C8), §5-2(C9), §2-1(C10), §1-10(C11·C12), §0-1(C16), FOR_BMS(C15), INTRO, HANDOFF(C14·C17), README, WORKING_STATE.
5. 테스트 사각지대(C20): SCOPE_DOCS 전체 숫자 대조, 철회 문구 금지 목록, sha 대용량, `_interp_lin_extrap` 중복 x 가드.
6. 사용자 기계: `ne_shape.py` 재실행(C18), 고친 비교기로 192 값 CSV 재대조(C3·C4), profile 재실행 또는 "수렴 미확인" 표시(C5).
7. 요청문 R3.

## 유지되는 것 (리뷰 셋이 다 재현한 것)
13 적합 폭 순위 집계 · raw LLI 반폭 10.5x(max/max, 하한끼리) · §1-10 4/4 순위(절대 폭, 탐색 하한 기술로서) · §2-1 부호 산술(97 행 |Δ| 8.95e-13, 강한 음수 5/5 대상 ub·기준 자유) · §5-2 (a)(b)(c) 수치 · 겹침/분리 8 행(외곽 범위로서) · 39 passed · Octave 4종 통과.
