# C-12 v40 — Codex 판정 기록 (R4 소스 · R5 배포본 실물)

> ⚠ 이 파일은 **기록 전용**이다. `IDENTITY_v40.json` 은 1저자가 Codex 에 보낸 실물이므로
> **바이트를 바꾸지 않는다** (그 파일 sha256 `7924de97…`). 이후 판정은 여기에 적는다.

## 대상

```
ZIP       9621a438919a0e32083feb8a32587633e426ba52e2196b37399bdd2b19ff92b0  (679,689 bytes)
MANIFEST  a3121ba7517564216fef6b43758ee79369a35ac5d68a5a24f4eb8747adcac1bc
커밋      e992da738da0b7b9d1e9abe1cf792714cdb372b4
생성기    684caa44378c1e9433c9f63eac35c265ea8c5d3e88e906b30daedfaaf4dce95b
```

## R4 (2026-09-08 · 소스 재검토) — 문의 발송 GO · 생산 실행 보류

- v39 P1(메모리 3상태) **소스 조건부 해제**. 범위: Slurm 통상 마운트에서 실제 적용되는 조상 제한 파일을
  읽을 수 있는 환경. *"다른 배치는 전부 미관측으로 정지"* 를 **일반 보증으로는 승인하지 않음** — 일부 계층
  읽기 실패가 다른 계층의 finite/max 에 가려질 수 있고, `cgroup.controllers` 만으로 namespace 밖 조상을
  확인한 것은 아니다. 그리고 `unlimited` 는 스케줄러가 CPU·메모리를 **예약했다는 증거가 아니다**.
- P2-1(문의문) 해제 · P2-2(CLI 주표시) 소스상 해제. 후속 문구 2건은 커밋 `a734daeb1` 에서 반영
  (`_recommend()` 의 "천장은 결정론" → NELM 횟수 상한·시간 보장 아님 · 문의문 25행 중립화).

## R5 (2026-09-08 · 배포본 실물 대조) — **배포본 대조 GO**

R4 가 남겨 둔 *"소스와 실제 배포본이 같은지 미확인"* 조건이 닫혔다. 추가 P0/P1 없음.

| 항목 | 독립 대조 결과 |
|---|---|
| ZIP · MANIFEST | 679,689 bytes · 두 SHA256 모두 일치 |
| 배포 파일 | 등록 129파일 전부 해시 일치 · MANIFEST 포함 130 · 등록 밖 추가 파일 없음 |
| 잡·상 | 19잡 · static 19 · 1단계 12 / 2단계 7 |
| 계산 입력 | 검증된 v39 리뷰 사본과 POSCAR/INCAR/KPOINTS **57파일 바이트 동일** |
| 변경 범위 | v39 대비 MANIFEST·README·SUBMIT·run_staged.sh 만 |
| POTCAR 원문 | 0파일 |
| 배포 스크립트 | 분석기·census·러너·봉인·증서 도구 해시가 IDENTITY 와 일치 |
| 소스 대조 | 생성 커밋의 `RUN_STAGED` 문자열과 배포 `run_staged.sh` 가 **바이트 동일** (AST 문자열 상수 대조 · 생성기 재실행은 안 함) |
| 문법 | 단계 러너·대표 잡 러너 `bash -n` exit 0 |
| 문서 정합 | 4.46일 · 단계당 156 h · 중앙 55.6/51.5 · NELM 148.2/137.4 가 MANIFEST 와 일치 · return_contract 17항목 · POTCAR 제외 명령 일치 · v36 대체 안내 · 답변 전 시작 금지 |

**R4 의 조건부 해제가 이 ZIP 의 러너에도 그대로 들어 있음이 확인됐다** (미관측 중단 · finite/unlimited/unobserved
구분 · 근거 기록). 승인 범위는 R4 와 같다.

### R5 가 검증하지 **않은** 것
- 전체 selftest · Linux 실행 · 프로브 실물 배치/메모리 · PP 트리 · VASP build · 스케줄러.
- 배포 `census.py` 를 Windows Python 으로 돌린 결과는 exit 1 (경로 구분자 차이). 독립 정규화 검사에서 실제
  파일/잡/단계 불일치는 없었고, Codex 는 이것을 **Linux census PASS 로 보고하지 않았다**. Windows 이식성은
  승인 조건이 아니다 (인수처는 Slurm/Linux).
- 인수처의 실제 CPU·메모리 예약 증거를 대신하지 않는다.

## 남은 것 — 현장 계약뿐

1. 16노드 · 노드당 48랭크에 대응하는 **실제 CPU 예약량 · 물리코어/SMT** 조건
2. **노드별 실제 할당 메모리 · 적용 cgroup 제한** 과 이번 환경에서의 관측 가능 여부
3. **단계당 156 h 이상** 할당 가능 여부, 또는 완료 잡 사이에서 안전하게 **이어갈 실제 운영 방식**

세 질문은 2026-09-08 카톡으로 이미 발송했다 (`INQUIRY_queue_2026_09_08.md` 의 카톡용 블록).

## ⛔ 발송 규칙 (현행)

- **ZIP 외주 발송·생산 실행은 위 세 답을 받은 뒤.** 코드·배포본을 이유로 다시 고치는 단계는 끝났다.
- 코어 수·노드 수·동시잡 수가 바뀌면 그 조건으로 자원·시간 계획을 **다시 계산**한다 — 4.46일·156 h 를
  그대로 이월하지 않는다 (그 경우 새 번들 v41+).
- v40 README 에는 "시작 전 확인·회신" 절이 **없다** (그 절은 커밋 `2711bea1e` 이후 생성분부터). 질문은
  카톡으로 나갔고, 메일 본문에 *"이 답을 받기 전에는 시작하지 말아 주십시오"* 가 있다.
