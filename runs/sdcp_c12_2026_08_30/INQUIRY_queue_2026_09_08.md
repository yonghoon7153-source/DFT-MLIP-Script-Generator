# 건엽 문의 — 큐·운영 방식 (Codex v38 재검토: 문의 발송 GO · 생산 실행은 보류 · **v39 재검토 P2-1 반영 2026-09-08**)

> 목적: 아래 질문의 답을 받기 전에는 **어느 판도 돌리지 않는다.** 이 문의는 번들 발송이 아니다.
> 배경 숫자는 v39 MANIFEST `cost_frozen.stage_alloc_h` (러너 실제 순서로 재계산한 값) · `memory_model` (모형값).
> v39 P2-1 반영: ① "최악(NELM 다 돌면)" → 시나리오 문구(보장 아님) ② 노드별 CPU 예약·할당 메모리·cgroup 제한 질문 추가
> ③ 292/73 GB 는 **모형값** 표기 ④ 코어·동시잡이 바뀌면 재계산 문구. **Codex R4 (2026-09-08): 문의 발송 GO** — ZIP 은 여전히 미첨부.

---

## 카톡용 (짧게)

```
건엽아, 지난번 OOM 원인 잡았고 새 판 만들었어. 보내기 전에 두 가지만 확인 부탁.

1) 노드 배정
   잡 하나를 노드 4개에 펼쳐서 동시 4잡 → 총 16노드 필요.
   (한 노드에 몰면 최악 잡이 노드당 292 GB 를 요구하는 걸로 나와서 터졌던 거야. 4노드면 노드당 73 GB.
    이 292/73 GB 는 실측이 아니라 **지금 우리 모형값**이야 — 73 GB 가 예약 요구량이나 안전 보장은 아냐.)
   16노드 동시 확보 가능해? 안 되면 몇 노드까지?

1-b) 노드별 자원 — 단위까지
   잡당 192랭크를 4노드에 나눠 노드당 48랭크로 돌릴 계획이야.
   그에 맞는 노드별 CPU 예약량(물리코어/SMT 구분)과 실제 할당 메모리·cgroup 제한을
   단위까지 확인해 줄 수 있어? 현재 메모리 계획은 노드당 188 GB야.
   (노드별 제한 확인이 필요해서 실제 값을 알려줘.)

2) 시간 — 이게 중요
   러너가 한 할당 안에서 단계 전체(1단계 12잡, 2단계 7잡)를 돌려서,
   잡당 시간이 아니라 **단계당 할당**이 필요해.
   중앙 추정은 55.6/51.5시간이고, NELM=200 시나리오는 148.2/137.4시간이야.
   스텝당 시간 모형이 ±2배라 보장된 상한은 아니고, 156시간은 계획 요청값이야.
   → 단계당 **156 h 연속 할당** 요청하려는데, 잡당 큐 상한이 91 h 라고 했잖아.
   ① 156 h 짜리 할당이 가능한 큐/파티션이 있어?
   ② 없으면, 끝난 잡 사이에서 다음 할당으로 이어갈 수 있게 해주는 방식이 있어?
      (잡을 중간에 끊거나 쪼개는 건 안 되고 — static 단일점이라 재개가 없어)

코어 수·동시잡 수가 지금 계획(192코어/잡 · 동시 4잡)과 달라지면 시간·자원 계획을 그 조건으로
다시 계산해서 보낼게 — 지금 숫자(4.46일 · 단계당 156 h)를 그대로 옮기진 않을 거야.
답 오면 그에 맞춰 최종판 보낼게. 지금 있는 파일은 돌리지 말고 기다려줘.
```

## 답을 받으면 적을 곳

- `db/properties/c12_site_contract_<날짜>.json` (신설) — 노드 수 · **노드별 CPU 예약(물리코어/SMT) · 할당 메모리 · cgroup 제한(단위 포함)** ·
  큐 상한 · 연속 할당 가능 여부 · 이어가기 방식.
- 코어·동시잡이 계획과 다르면 `vasp_cost_estimate.py --manifest … --cores <c> --concurrent <m>` 로 **재계산**한 값을 v40+ README 에 넣는다
  (4.46일 · 156 h 를 그대로 옮기지 않는다).
  이 파일이 v39 README 의 "큐 확인" 문장을 대체한다 (확인 안 된 채로 발송하지 않는다).
- 답이 "156 h 불가 · 이어가기도 불가" 면 → 단계를 물결 단위로 나누는 **별도 사전등록**이 필요하다
  (러너가 임의로 쪼개면 1단계 정지 규칙이 깨진다). 그건 1저자 결정.

## 하지 말 것

- 이 문의에 ZIP 을 붙이지 않는다 (재승인 전).
- "84 h" 는 어디에도 쓰지 않는다 — 철회된 잡당 값이다.
- "77 h 보장" · "결정론" · "최악" 표현 금지 — NELM 시나리오는 상한 보장이 아니다 (v39 P2-1).
- 292/73 GB 를 실측처럼 쓰지 않는다 — 모형값이라고 붙인다.
