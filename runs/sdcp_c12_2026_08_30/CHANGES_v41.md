- ⚠ **계산 입력은 v40 과 동일합니다** — 잡 목록 19개(1단계 12 · 2단계 7)·INCAR·POSCAR·KPOINTS·POTCAR 규격·
  사전등록이 그대로이고, 잡 집합 지문(job_keys sha16 `8a7ae28f`)이 v40 과 같습니다. 물리적으로 같은 계산입니다.
- **바뀐 것은 실행 전 검사 스크립트 두 개뿐입니다** — `census.py` 와 `run_staged.sh`.
  v40 은 러너가 스스로 만드는 작업폴더 `_hostpool/` 을 **잡 폴더로 세어** "잡 폴더 수 ≠ 계획" 으로
  **첫 VASP 실행 전에 멈췄습니다.** 저희 쪽 결함이며 귀측 실행과는 무관합니다. `_` 로 시작하는 폴더를
  잡 집계에서 빼도록 고쳤고, 같은 상황을 재현하는 시험을 배포 selftest 에 넣었습니다.
- v40 으로 시작하셨다면 census 단계에서 멈춰 **VASP 잡은 0개 실행**된 상태가 정상입니다 — 남은 산출물은
  없으므로 v40 폴더를 지우고 v41 로 **처음부터** 같은 절차로 진행해 주시면 됩니다.
- **README 맨 앞과 이 메일 §0 에 "시작 전에 확인·회신해 주실 것" 절이 들어 있습니다** (v40 README 에는 없었습니다).
  내용은 2026-09-08 에 카카오톡으로 드린 **다섯 가지 질문과 같습니다** (노드 확보 · 노드별 CPU/메모리/cgroup 제한 ·
  단계당 할당 시간 · 이어가기 운영 방식 · 실행 환경). 이미 답을 주셨다면 다시 답하실 필요 없이 기록용으로 보시면 되고,
  아직이시면 답을 주신 뒤 시작해 주십시오 — 답에 따라 코어·동시잡·walltime 을 다시 계산해 새 번들을 드릴 수 있습니다.
- 그 밖의 실행 절차·반송 목록·게이트·walltime 계약은 **v40 과 동일**합니다. 새로 익히실 것이 없습니다.
