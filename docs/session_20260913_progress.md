# 세션 진행 2026-09-13 — DEM 스택 L1~L5 판정 대응 + 면적 계약

> 규율 ③ 대피소.  **한정어를 깎지 않는다** — 우리 가치는 "하한", "Physics 가지 전용",
> "동일 프로세스 한정" 같은 말에 있다.
> 정본은 여전히 `docs/reviews/findings.json` (원장) · `docs/area_contract_20260913.md` (계약) ·
> 판정문 5편.  이 문서는 **오늘의 수치·판정**을 모아 둔 것이다.

## 1. 오늘 닫은 것 — A·B·C 무더기 13건 + 앞선 C 2건

| 커밋 | 무엇 |
|---|---|
| `33524721` · `e0ce6bf6` | **C**: `L5-03`(금지 열이 학습됨) · `L5-05`(대조 전용 산물이 배포 모델로) |
| `6b00e6e5` · `f1b03bf6` | **A**: `L3-11` 설명 ↔ 상수 정합 (세 세대 공존) |
| `ea11cdaf` | **A**: `L3-06` CLAUDE.md 의 EXCL 23 → **25** |
| `03a5ddec` · `a465d35f` | **B**: `L4-03` · `L5-07` · `L3-10` |
| `63d17bd5` · `3909422b` | **B**: `L3-08` · `L3-12` · `L4-06` · `L4-07` · `L4-11` |
| `62e67e02` · `f993c2eb` | **A**: `L2-07` · `L2-08` · `L4-04` · `L4-09` · `L1-04`(설명층) |

**재현한 수치 (원장 note 에 전부 있음, 여기는 사본):**
- `L3-08` n=12·전부 EXCL → `n_fit 0` · `r2 0.0` · `loocv 0.0` · `pred[0] = 0.07688995743054786`
- `L3-12` 옛 키 `(20, 200, 42, -1.0, 1.0, 30.0)` 가 중간 y ×10 에도 **동일**
- `L4-06` atoms **84 B** · contacts **33 B** 로 크기가 같은 두 사례 중 하나가 사라짐
  (⚠ 어느 쪽인지는 **rglob 순서**에 달렸다 — 판정문 `case_a` / 내 재현 `case_b`)
- `L4-07` 실제 type2 입자 5개 → **추론 SE 0개** (3-type 기본 지도)
- `L4-11` porosity **1** → `R_ct 128462895.60746863` · **1.000001** → `4.805944515715439`.
  **수정 후 1 % = `4.805944467170544`** = 판정문 값과 일치, 1 에서 연속
- `L2-08` 같은 망에서 통계 **45 %** ↔ 실제 소산 몫 **8.1818 %** (5.5배)
- `L2-07` 같은 4구에서 **CF/FULL 3.6477 ↔ Brug/FULL 0.2771** (하나는 1보다 크고 하나는 작다)
- `L4-04` 모든 σ·grain ×4 → 앱 τ **3.464101615** 불변, grade **3.464101615 → 1.732050808**,
  등급 **B− → A**
- `L4-09` 85:15 실제 반환 **148.75** (설명은 162 = 190 계열이었다)
- `L1-04` `A_LIGG/A_Hertz = 2 − d/(2r)` → r=0.5 µm·δ/R*=0.05 에서 **1.9875배**

**새 상주 검사기 일곱** (전부 check_all + CI + 등재):
`audit_validation_flags --selftest`(L4-03) · `gen_plots --selftest-descriptions`(L3-11) ·
`gen_plots --selftest-fits`(L3-08·L3-12) · `eis_drt_ica --selftest`(레인 none → both 승격,
L4-11) · `extract_se_network_diagnostics --selftest`(L4-06·L4-07) ·
`grade_engine --selftest`(A군 라벨) · `audit_constriction_deleted --selftest`(S0).

**판정문이 짚지 않았는데 그 자리를 고치다 나온 것 넷**: `SELF-26`(전자 σ 캐시 둘은 키가
**아예 없었다**) · `SELF-27`(`porosity or 0.15` 가 0 을 결측으로) · β_AC 가 구조적으로 0 인데
적합 결과로 인쇄되던 것 · **`SELF-28`**(아래).

⚠ **A·B·C 는 저장된 산출물의 숫자를 바꾸지 않았다.**  유일한 예외가 `L4-11` 이고 움직이는
구간은 **API 가 허용하는 porosity 0.1–1.0** 뿐이다 (거기서는 옛 읽기가 틀렸다).

## 2. SELF-28 — 협착 항이 **삭제**된다 (오늘의 새 실측)

계기 = 사용자의 *"2πR_min² 에 걸리는 값이 전혀 없었다, 명목상 cap 이었다"*.  코퍼스로 확인:
**반대였다.**  ★ 다만 기억이 맞는 대상이 따로 있다 — `A_volume` 과 `hertzian` 이 **0/163**
(최대 0.000 %) = 진짜 명목뿐인 cap.

**결정론적 연쇄 (산술, 반지름 무관):**
```
binding='geom' ⇒ A = 2π·r_min² ⇒ a = √(A/π) = √2·r_min   (network_conductivity.py:323)
               ⇒ a > r_min → clamp (:395) ⇒ a_eff = r_min 정확히
               ⇒ ψ = (1−1)^1.5 = 0 (:396) ⇒ R_constriction = 0 (:399-401)
```
(`r_min_real = min(r1,r2)` `:386` 이 `A_geom` 의 `R_min` 과 **같은 양**이라 비가 정확히 1)

**코퍼스 실측** (`docs/data/case_master.csv`, 163 케이스, 접촉 38,157,976):
`geom` 결속이 **163/163 케이스 전부 > 0** · 접촉 가중 전체 **3.966 %** (1,513,533) ·
**AM–SE 13.843 %** (1,243,608) · 케이스별 AM–SE **1.4 ~ 42.5 %**.

**실제 함수 사다리** (r_SE 0.5 ↔ r_AM 6.0 µm, ligg_area 0):

| δ (µm) | binding | ψ | R_constriction |
|---|---|---|---|
| 0.02 | volume | 0.6239984308 | `5.9403496296` |
| 0.05 | tabor | 0.2691568345 | `6.3713479493` |
| 0.10 | tabor | 0.0026814364 | **`380.2740936428`** |
| 0.12 | **tabor** | **0** | **`0`** |
| 0.15 | **tabor** | **0** | **`0`** |
| 0.20 | geom | 0 | `0` |

⇒ **ψ→0 시작 δ = 0.10234 (binding=tabor)** · **geom 결속 시작 δ = 0.16292**
⇒ **AM–SE 13.843 % 는 하한**이다.  진짜 값은 **아직 미측정** (S0 이 그것을 잰다).

⚠ **Physics 가지 전용** (Hertz-명명 가지엔 clamp 자체가 없다).  단 **σ_thermal Stage T1 의
타깃이 그 가지**이고(`thermal_sigma_full_mScm_stage_e_physics`) 특징 10·12·14 도 `*_physics`.
⚠ 두 산출 경로가 **같은 인자로 같은 함수**를 부른다 (`network_conductivity.py:299-302` ↔
`coverage_physics_vs_hertzian.py:230-232`) ⇒ 결속 비율 = 솔버가 보는 비율.

### 2-1. 내가 세 번 고친 서술 (기록으로 남긴다)
1. *"cap 은 `a > r` 을 만든 장본인이고 clamp 는 반창고"* → 틀림
2. *"반창고가 아니라 스위치"* → 부분적으로 맞음 (clamp 가 ψ 를 0 으로 만드는 것은 사실)
3. **현재: "cap 은 원인이 아니다"** — cap 을 **빼도** δ=0.12·0.15 에서 ψ 가 이미 0 이다
   (`tabor` 가 먼저 넘는다).  빼면 면적만 커진다.
⇒ 세 번째도 틀릴 수 있다.  Codex 리뷰에 **이 세 번의 이력 자체**를 같이 보냈다.

## 3. 면적 계약 (E 결정) — `docs/area_contract_20260913.md`

**결정 = (a) 두 입자 사이 실제 전류/열이 통과하는 patch.**  면적을 새로 만드는 것이 아니라
**지금 하나인 것을 둘로 쪼갠다.**

| | `A_surface` (피복) | `A_transport` (수송) |
|---|---|---|
| 상한 | **2π·R_min²** (반구 = wraparound) | **π·R_min²** (⟺ `a ≤ R_min`) |
| 소비자 | coverage · 반응면적 · B3 | `network_conductivity` 협착 항 |
| 지금 | **검증됨** (16/49 % ↔ Hertz 18 / Tabor 52) | **없다** — surface 를 빌려 쓰는 중 |

**cap 은 결함이 아니다** — 코드가 *"Hemisphere cap prevents wraparound"* 라 적었고 그 목적에서
독립 기하 ground-truth 로 검증됐다.  결함은 **한 변수가 두 공간을 산다**는 것 (coverage 는
`4πr²` 로 나누고 솔버는 `√(A/π)` 로 읽는다).

**순서 S0~S4 · 사전등록 §5** (런 **전에** 등록, 결과를 보고 안 바꾼다):
h0 = Physics σ_ion **3 % 미만** 이동 / h1 = **10 % 이상** / 분해능 **5 %** ·
부호 예상 **하향** · 음성 대조 = coverage(Hertz·Tabor) **셀 단위 완전 동일**.
⛔ **재적합은 승인하지 않는다** — σ 가 움직이면 **세대 2** 로 따로 적는다.

## 4. 지금 막혀 있는 것

- **S0 미실행** — `scripts/audit_constriction_deleted.py` (CPU, 솔브 없음, `--webapp` 지원).
  이 컨테이너에 `contacts.csv` 가 없고, 사용자 WSL 의 `find ~ -maxdepth 5` 도 **0건**.
  ⚠ `find` 는 기본적으로 **심볼릭 링크를 안 따라간다** — CLAUDE.md 가 *"데이터 → WEBAPP_*_FOLDER
  **연결**"* 이라 적었으므로 `-L` 로 다시 봐야 한다.  코드 트리는 `~/dem-web` · `~/ms-review`,
  `~/Yonghoon-DEM-DFT` 는 데이터 쪽으로 보인다.
- **Codex 리뷰 대기** — 주장 A(산술)·B(코퍼스)·C(사다리) + 계약 (a) 선택 + 사전등록 반증가능성
  + S0 도구 자체.
- **D 무더기 21건** — 대표 raw case 고정 → 수정 전후 재추출 → 세대 분리가 선행.
- **비포/애프터 웹앱 페이지** — 초안(384줄)이 스크래치패드에 있으나 **B·A 13건 수정 전 상태**
  ("고침 7 / 열림 57" 로 적혀 있고 실제는 고침 20).  갱신 필요.

## 5. 오늘 배운 규율 (재발 방지)

- **엉성한 필터로 판정문을 반박하지 않는다** (`SELF-25` 재현) — 리터럴 175 잔존 검사를
  정규식으로 짰더니 **자기 자신**(상수 정의·검사 문자열)까지 세어 거짓 빨간불을 냈다.
  **AST 로 실물을 잡아** 고쳤다.
- **조회 키를 라벨이라고 착각하지 않는다** — `'Constriction 비율(%)'` 은 app.py 두 곳 ·
  single.html 두 곳이 같은 문자열로 찾는 **키**였다.  바꿨다가 되돌리고 해설 쪽에서 고쳤다.
- **코드 폴더 ≠ 데이터 폴더** — S0 초판이 다른 체크아웃에서 돌면 사례 0개로 **빈 집계**를
  냈다 (규율 ⑤ false-green).  `--webapp` + rc 1/2 fail-closed 로 막았다.
- **픽스처 실수도 대조로 남긴다** — 랭크 검사를 시험하려고 **드롭된 열**을 겹쳤다가 빨간불을
  냈다 (드롭 열은 LIVE 랭크와 무관).  그 대조를 selftest 에 남겼다.
