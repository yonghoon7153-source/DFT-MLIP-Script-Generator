# L1~L5 지적 수정 계획 (2026-09-13)

> 판정 = `docs/reviews/codex_verdict_dem_stack_L1_20260913.md` ·
> `docs/reviews/codex_verdict_dem_stack_L2_20260913.md` ·
> `docs/reviews/codex_verdict_dem_stack_L3_20260913.md` ·
> `docs/reviews/codex_verdict_dem_stack_L45_20260913.md` (다섯 층 전부 HOLD) +
> `codex_verdict_lhs_descriptors_20260913.md`.  원장 207건 중 이 다섯 층 소관 **31건**.
>
> ⛔ **다섯 판정문이 공통으로 그은 경계**:
> *"구현 정합성 수정과 과학적 모델 선택을 한 커밋/한 성과 숫자로 섞지 않는 것이 해제 조건"* ·
> *"기존 정본의 수치를 새 함수로 덮어쓰는 것은 금지"* · *"기존 산출물은 지우지 말고 **계산 세대를
> 나눈다**"* · *"LIVE 계수 재적합도, LOCKED 지수·EXCL·φ_c 변경도 승인하지 않았다"*.

## 무더기 나누기 — 무엇을 먼저 할 수 있나

| 무더기 | 성질 | 계산이 바뀌나 | 지금 할 수 있나 |
|---|---|---|---|
| **A. 라벨·설명** | 계산은 그대로, **보고하는 이름**만 실제와 맞춘다 | ✗ | ✅ 즉시 |
| **B. fail-closed 추가** | 지금 조용히 통과하는 것을 거부한다.  정상 경로 불변 | ✗ (정상 입력에) | ✅ 즉시 |
| **C. 우리 DO-NOT 강제** | 문서에만 있던 금지를 코드로 옮긴다 | ✗ (금지 대상 외) | ✅ 즉시 |
| **D. 상태 계약** | 정상 0 · 미측정 · 실패 · estimate 를 가른다 | **✓ 바뀐다** | ⚠ 세대 분리 후 |
| **E. 물리 모델 결정** | *"L2 에 넘길 면적 A 가 무엇인가"* 부터 | **✓ 크게** | ⛔ **저자 결정 선행** |

★ **A·B·C 는 판정문들이 *"기존 구성 안에서 고칠 수 있다"* 로 직접 나열한 것**이다.
D 는 계산이 바뀌므로 **대표 raw case 를 고정해 수정 전후를 재추출**하고 세대를 나눈 뒤에 한다.
E 는 내가 정하지 않는다.

---

## A. 라벨·설명 — 계산 불변 (**원고 직결**)

판정문: *"이는 문법 취향이 아니라 **어느 수식을 계산했다고 보고하는가**의 결함이다."*

| ID | 무엇 | 실제 ↔ 설명 |
|---|---|---|
| `L3-11` | 전자 plot 제목에 **드롭된 4항**이 남음 | 계산 8 LIVE ↔ 제목 12항 |
| `L3-11` | 전자 CSV 열 이름 | 값은 22.5 ↔ 이름 `Stage_15_form` |
| `L3-11` | 열 특징 수 | **14** ↔ `G:6703` **18** · `G:6911` **16** |
| `L3-11` | 열 Ridge α | **0.05** ↔ `G:6911`·`G:7039`·`G:6694` **0.1** |
| `L3-06` | EXCL 개수 | **25** ↔ report **23** (CLAUDE.md 는 `ea11cdaf7` 로 정정 완료) |
| `L4-09` | `Q_gravimetric` 설명 | 실제 **175** ↔ 설명 **190** |
| `L2-08`·`L4-04` | *"협착이 전체 저항의 45 %"* | 실제는 **접촉별 비율의 비가중 평균**, 전력 몫은 8.1818 % |
| `L2-07`·`L4-04` | `CONTACT_FREE/FULL` 설명 | **모델 내부 비교** ↔ *"Bruggeman 의 실측 대비 과대율"* |
| `L4-04` | grade τ 라벨 | **Laplace eff/bulk** (σ_grain 3.0 고정) ↔ Dijkstra 아님 |
| `L1-04` | `*_hertz_pct` | **기하 교차 원판** ↔ Hertz 탄성 |

⚠ `L1-04` 의 웹앱 쪽 **키 개명은 여기 넣지 않는다** — 판정문이 *"기존 키와 frozen feature 를
**세대 표시 없이** 바꾸면 안 된다"* 고 했다.  **설명에 한정**한다.

## B. fail-closed 추가 — 정상 입력에는 불변

| ID | 지금 조용히 통과하는 것 |
|---|---|
| `L4-03` | audit 가 `"false"` 문자열·개별 실패·JSON 깨짐을 통과시켜 **3/3 trustworthy** |
| `L4-06` | 진단 dedup 이 **바이트 수만** 봐서 다른 사례를 지운다 |
| `L4-07` | `input_params` 가 있으면 **meta 의 type_map 을 무시** |
| `L4-11` | `porosity` 를 **값 크기로 %/fraction 추측** → 1 에서 불연속 |
| `L5-07` | **NaN 이 범위·외삽 가드를 통과**해 정상 배포 표지 |
| `L3-08` | EXCL 후 **n_fit = 0 인데 유한 예측·R²=0** |
| `L3-10` | 열 plot CLI 가 PNG 뒤 **KeyError rc=1** (`desc` ↔ `description`) |
| `L3-12` | 이온 bootstrap **캐시 키가 중간 y 를 안 본다** |

## C. 우리 DO-NOT 을 코드로 — 문서에만 있던 것

| ID | CLAUDE.md 의 문장 | 코드 현실 |
|---|---|---|
| `L5-03` | *"`use_porosity_pct` 는 **학습·노출 금지 열**"* | **학습되고 `suggest_batch` 로 나간다** |
| `L5-05` | *"되돌리려면 `--derived-products` (**대조 전용**).  ★DO NOT 되돌리지 말 것"* | 그 산물이 **USABLE 배포 모델**로 수용 |

⇒ **이 둘이 제일 아프다** — 우리가 규약으로 적어 둔 것이 강제되지 않았다.

## D. 상태 계약 — 계산이 바뀐다 (세대 분리 후)

`L2-02`(정상 미퍼콜 → 양수 fallback) · `L2-03`(unity-skip 이 파괴 무시) ·
`L2-04`(벽 fallback) · `L2-05`(전극 g 가 전 간선 sum/max) · `L2-06`(상태 분류) ·
`L2-09`(채널 밖 접촉) · `L2-10`(0 저항 삭제) · `L3-01`(정상 0 → raw 양수) ·
`L3-02`(AM gate 두 규약) · `L3-03`(cov Physics 대체) · `L3-04`(보조 검증기) ·
`L3-05`(fold 밖 imputation) · `L3-09`(`f_intact` 0 = 100 %) ·
`L4-01`(정상 미전도 = 0 Ω) · `L4-02`(source 가 등급을 안 막음) · `L4-05`(0면적 edge) ·
`L4-08`(cycle-stable marginal) · `L5-01`(σ 로 코호트 선별) · `L5-02`(loader 변조) ·
`L5-04`(closure 누출) · `L5-06`(UI `d_am`) · `HARV-01` 잔여(τ 가 `plate_z` 미수신)

⚠ **`L3-02` 는 D 안에서도 특별하다** — *"어느 규약을 정본으로 택하든"* 두 호출군이 다르다는
것만 확정됐다.  **어느 쪽이 등록된 T1 인지**는 저자가 정해야 한다.

## E. 물리 모델 결정 — 저자 선행 (⛔ 내가 정하지 않는다)

판정문 L2 §1 의 **권고 결정 순서**:

1. **수송 접촉의 정의** — 두 입자 사이 **실제 전류가 통과하는 patch** 인가, **입자 표면의
   피복** 인가, **퍼진 막의 추가 면적** 인가.  (분리된 여러 patch 의 총면적만으로 협착저항이
   유일하게 정해지지 않는다.)
2. **접촉 연산자와 영역** — 작은 원판의 반공간 모델인가, 유한한 두 입자/neck/막을 푸는
   모델인가.  `a`·`b`·재료쌍·pair conductivity·입자 bulk 항과의 경계 분할을 함께 명시.
3. 그 계약에 맞춰 **L1 의 SI 일관성·lens·재료쌍별 상수·면적 제약**을 고친다.
4. 두 입자·균일 neck·극한에서 **연속성·단조성·단위변환·독립 전도해**를 확인한 뒤 bed 로.

해당 ID: `L1-01`(cap feasibility) · `L1-02`(lens 정의) · `L1-03`(pair 물성) ·
`L1-08`(5 nm 앵커) · `L2-01`(ψ 위치 **와** 면적 A 의 뜻) · `L1-05`(삼중 겹침 union) ·
`L1-06`·`L4-08`·`L4-10`(다른 측정이 필요한 부류).

★ `L2-01` 의 **ψ 를 곱셈으로 바꾸는 것은 필요한 정정**이지만, 판정문이
*"그것만으로 '물리 모델 검증 완료' 라고 쓰면 안 된다"* 고 못 박았다.

---

## 이번 회차 = **A + B + C** 만

⛔ D 는 **대표 raw case 고정 → 수정 전후 재추출** 이 선행이고, E 는 **저자 결정** 이 선행이다.
⚠ A·B·C 는 **기존 저장 산출물의 숫자를 바꾸지 않는다** — 라벨·거부·금지뿐이다.
각 항목은 **회귀를 먼저** 넣고 고친다 (규율 ②).
