# 세션 진행 2026-09-11 ~ 09-12 — litdb 계보 5장 완주 · 압력축 판정 · ban-sweep 사각 발견

> 정본 아님.  판정·규약의 정본은 `CLAUDE.md` 와 `docs/reviews/*`.  이 문서는 **오늘의 수치·판정 대피소**다.

## 1. litdb 정본 브랜치(`claude/friendly-meitner-lldvar`)에 5장

| 카드 | 커밋 | 성격 |
|---|---|---|
| `vijay2025_hybrid_cgmd_dl_slurry_microstructure` | `d36bedcad` | 신규 |
| `galvezaranda2024_time_dependent_dl_calendering_microstructure` | `aac1a2bf8` | 신규 |
| `galvezaranda2025_paml_vgg16_dem_slurry_drying` | `a104784b3` | 신규 |
| `ngandjong2021_dem_calendering_digital_twin` | `0f555ccd3` | **2판** (200 → 757줄) |
| `xu2023_realistic_am_shape_cgmd_calendering` | `2270fab36` · `804270465` | 신규 (846줄) |

⚠ 중복 판정은 전부 `git ls-tree FETCH_HEAD litdb/papers/ --name-only` **전수**로 했다.
`ngandjong2021` 은 `INDEX_DEM.md` 에만 있고 `INDEX.md` 에는 **없었다** — CLAUDE.md 가 적어 둔
"인덱스가 하나가 아니다" 함정이 이번에도 실재했다 (xu2023 커밋에서 `INDEX.md` 구멍도 닫았다).

## 2. ★ 압력축 판정 — 이 계보에 우리 300 MPa 로 옮길 압력값은 **0개**다

- `ngandjong2021` 은 계보에서 **유일하게 MPa 축**을 갖는다: 실험 5점 · 시뮬 7점, **P_max = 156 MPa**.
  우리 Heckel knee `P_y = 138 MPa` 바로 위에서 끝나고 생산점 300 MPa 의 **52 %** 다.
  densification 의 63 %가 5.92 MPa, 99 %가 86 MPa 안에서 끝나 **post-knee 구간에 점이 0개**.
- `xu2023` (후속편, 물리 엔진 원점)은 본문·SI 통틀어 **`MPa` 0회**.  공정 축이 두께감소율(CD %)
  하나로 바뀌었다.  ⚠ 압연기(BPN250)·라인속도(0.54 m/min)·롤온도(60 ℃)가 `ngandjong2021` 과
  **동일**한데 그쪽이 쓴 FlexiForce 갭→압력 환산을 **안 했다** ("못 했다"가 아니다).
- ⛔ 두 카드를 이어 붙여 CD 에 MPa 를 붙이면 안 된다 — 같은 96:2:2 인데 압연 전 실측 porosity 가
  **42.15 %(ngandjong, stated) vs ≈47.5 %(xu, digitized) = 5.4 %p 차** ⇒ 다른 침대다.
- ⛔ **Heckel P_y 인용 금지** — `ngandjong2021` 점을 `ln(1/(1−D)) = K·P + A` 로 옮기면 창에 따라
  408 / 740 / 410 / 1,284 MPa 로 **3.1배** 흔들리고, R² 0.99 인 창은 자유도 1 이다.  게다가 그 ε 는
  **springback 후(out-of-die)** 라 in-die 규약인 우리 `P_y = 138` 과 같은 양이 아니다.
- ★ **살아남는 문장 하나**: *"floor 를 깨는 것은 압력이 아니라 기전이다"* — 86 → 156 MPa(1.8배)에
  Δε = **0.1 %p**.  frame[5] DEM↔MPM 분업에 대한, 우리 데이터가 아닌 **외부 실험** 근거다.
  ⚠ `xu2023` 의 20.4 / 25.0 % 는 **바닥이 아니다** (마지막 구간 기울기 dε/dCD ≈ −0.90 / −0.96 %p/%
  로 가파른 채 끝난다) — 강체구 floor 사다리에 넣지 말 것.

## 3. ★★ ban-sweep 사각 — 정본 litdb 브랜치는 스윕이 **원리적으로 못 본다** (SELF-18)

`claims.json` 의 `quotation_ban` 22 패턴으로 정본 `litdb/` 536 파일을 훑으니 **23 히트**,
그 중 **6 파일이 표지 없는 실사용**이다 (나머지는 철회 서사 자체이거나 무관한 우연 일치).
원장 항목 = `docs/reviews/findings.json` **SELF-18**.

## 4. ibb LHS

- 실측 정정: **다중코어 재시작이 빠르다는 증거가 없다** — 4~5코어 r3 들이 191~435 k스텝/일,
  1코어 r2 들이 214~483 k스텝/일로 **구분되지 않는다** (`OverSubscribe=OK` 로 노드가 이미 포화).
  ⇒ 유휴 코어를 쓰는 것 자체는 타당하나 "몰아주면 빨라진다" 는 근거가 없다.
- `lhsx_*_r2` 18개가 **09-12T15:03 동시 만료**(09-07 시작 + 5일 한도) — 그 시점이 실제 배분 결정점.
- 재시작은 `~/dem_test/dem_restart.py --case-list <CASE> --root ~/dem_test/lhs --suffix r3` 를 쓴다
  (⚠ 2026-09-12 에 내가 sed 로 같은 일을 새로 짰다 = 규율 ① "이 리포에 이미 있나" 위반).

## 5. Phase A vox 0.15 — 32/32 완주, 스칼라 회수, **판정은 거부**

- 캠페인이 09-12 10:32 에 **32/32** 로 끝났다 (중단 2회를 `phaseA_autorun.sh` 가 자동 재개).
- ⚠ **러너 산출물과 판정기 입력 스키마가 애초에 안 맞았다** — 러너는 웹앱 payload
  (`p2_*.json`, 팔당 150 MB, σ_e 가 `mpm_metrics.step3.sigma_e_eff_S_cm`)를 내는데
  `phase_a_order_verdict.py` 는 평평한 스키마를 읽는다.  판정기가 러너보다 먼저 쓰여
  (09-09) 그 사이가 비어 있었고, **32팔을 다 돌리고 나서** `키 없음` 으로 드러났다.
  ⇒ 어댑터 `scripts/phase_a_arms_from_payload.py` 신설 (selftest 15/15, 음성대조 10).
- ⚠ 기존 `reduce_arm_payloads.py` 는 못 쓴다 — **SBE/DBE 2역할 16팔 cohort 전용**이라
  4조성 × 8 origin 을 *origin 중복* 으로 보고 진단 모드에서도 거부한다.  **그 거부는 옳다**
  (표지 없는 부분 cohort 가 `p2_*.json` 이름으로 리포에 들어가는 것을 막는 장치).
- ★ 조성 축은 **원자료에 있다**: `mpm_metrics.additives.VGCF.wt_pct`.  파일명은 교차확인 전용.
- 회수한 팔 = `docs/data/phase_a_h015_arms/arms/` (32) + `run_receipt.json` + 어댑터 요약.

### 판정 = **REFUSED** (설계 64칸 공백)
등록 설계는 `{1,2,3,4} wt% × {0.25, 0.20, 0.15} µm × 8 origin = 96팔` 인데 이번 캠페인은
**vox 0.15 한 격자(32팔)** 만 돌았다.  ⇒ 판정기가 *"부분집합으로 판정하면 조용히 초록이
된다 (CL-71)"* 로 거부한다.  **ORDER-ROBUST 를 주장할 수 없다.**

### vox 0.15 단면 **측정** (판정 아님)
| VGCF wt% | n | 평균 σ_e [S/cm] | 팔-폭 | SE |
|---|---|---|---|---|
| 1 | 8 | 0.0021204 | 2.07 % | 0.211 % |
| 2 | 8 | 0.0152413 | 3.20 % | 0.431 % |
| 3 | 8 | 0.0607626 | 2.18 % | 0.281 % |
| 4 | 8 | 0.130968 | 0.84 % | 0.120 % |

인접 조성 쌍대응비 (같은 origin 끼리): **1→2 7.1878 ± 0.0224 · 2→3 3.9869 ± 0.0077 ·
3→4 2.1555 ± 0.0044** (전부 8/8 쌍).  구간이 **완전분리**된다 (다음 조성의 최소가 앞 조성의
최대보다 7.03 / 3.88 / 2.12 배 크다) 이고 **origin 8개 전부에서 단조 증가**다.
⇒ 이 격자에서 조성 순서는 origin 섭동에 흔들리지 않는다.  ⚠ **한 격자의 서술일 뿐**이고,
격자를 바꾸면 SR-01 에서 σ_ion 비의 **부호가 뒤집힌** 전례가 있다.

★ **plate rule 확인 완료 (2026-09-12, 삭제 전 회수)** — `run_receipt.json` 에는 그 칸이
없어서(`periodic_xy: false` 만 있다) payload 매니페스트를 전수로 읽었다:
**32/32 가 `plate_rule = p2-occupied-surface-first`** 이고 `periodic_xy` 도 32/32 가 False 다.
⇒ R5-CX-06 이 인용을 금지하는 것은 **p1** 산물이므로 이 32팔은 **그 축에 걸리지 않는다.**
전수 기록 = `docs/data/phase_a_h015_arms/phaseA_h015_manifests.json` (32팔 매니페스트 전문).
⚠ 다만 이것이 "인용해도 된다" 는 뜻은 아니다 — **판정은 여전히 REFUSED** 이고, 쓸 수 있는
것은 *"vox 0.15 에서 이렇게 측정됐다"* 는 서술뿐이다.
⚠ receipt 에 `plate_rule` 칸이 **없다는 것 자체**가 구멍이다 — 봉인 축을 receipt 만 보고
확인할 수 없었고, payload 를 지웠으면 영영 확인 못 했다.

## 6. kgy 디스크 (95 % · 여유 50 G)

`~/work` 552 G 는 DFT 라 건드리지 않는다.  우리 쪽에서 정리한 것:
- `~/sdcp` 4 디렉터리 **8.0 G** — 디렉터리명에 박힌 `receipt_digest` 가 리포의 커밋된
  축소 패키지와 일치해 중복이 증명된다 (`3137a340344b` · `ddb33647303c` · `d2be19ea80e4`
  · `2f46127f44a8`) — 넷 다 receipt digest 다.
  ⚠ 리포에 든 축소본은 **스칼라 패키지**라 시각화·재솔브 원자료는 아니다.
- `~/pa/phaseA_h015` 4.6 G — 스칼라 회수 후 삭제 가능 (위 §5).
- ⛔ `prereg_v2_vox04_*` · `prereg_v2_vox015` · `prereg_v2_vox015_sph` 는 이름에 digest 가
  없고 리포에 커밋된 대응본이 **없다** = 유일본이므로 지우지 않는다.

## 7. 원고 v6 · SI v6 의 시뮬 절 대조 (2026-09-12)

### ★ 헤드라인이 커밋된 원자료에서 **정확히 재현된다**
`docs/data/w4_ptfe_centerline_20260827` (8팔, vox 0.15, sphere 스탬프, PTFE centerline):
**SBE 53.987 · DBE 70.605 mS/cm · 쌍대응비 1.307820 ± 0.001053** (팔 범위 70.04–71.40).
원고 Table S3 의 `54.0 / 70.6` 과 일치하고 `plate_rule = p2-occupied-surface-first` 라
**R5-CX-06 의 p1 인용금지에 걸리지 않는다.**
⚠ 혼동 주의 — 같은 vox 0.15 의 **다른** 커밋 패키지
`cohorts/prereg_v2_vox015_sph_b048_ptscenterline_sbrg001_lean2_rd2be19ea80e4_reduced` 는
SBE 가 **같고**(53.987) DBE 만 다르다(71.279, 비 1.3203).  `sbrg001` 축이 SDCP 있는 팔에만
걸리기 때문이다.  ⇒ **원고 출처는 `w4_ptfe_centerline_20260827` 이고 cohorts 쪽이 아니다.**

### 지적 3건
1. **σ_SDCP: 런 250 · 표 256.6 — 저자 판단으로 종결** (SELF-20 `wontfix`).
   256.6 은 시뮬 **이후**에 나온 4점 탐침 실측이라 **정보의 시차**이지 설정 오류가 아니다.
   재실행하지 않는다 (2.6 % 는 비 감도로 1e-4 수준 = 결론 무관).
   ⬜ 남는 것은 **문장 하나** — 지금 표는 256.6 만 보여줘서 독자가 그 값으로 돌린 줄 읽는다.
   *"시뮬은 250 을 썼고 그 뒤 실측이 256.6 (2.6 % 이내)으로 확인했다"* 를 적으면 재현성
   구멍이 오히려 **사후 검증**이 된다.
   ★ 그리고 CL-61 의 약점이 닫힌다 — 그 클레임은 σ_SDCP 를 *"문헌 밴드가 없는 저자 지정값"*
   으로 두고 방어를 감도 스윕에 맡겼는데, 이제 **자체 실측**(Figure S5, 189 nm 필름)이 있다.
   ⇒ CL-61 의 방어 문장 갱신은 저자 판단 대기.
2. ⚠ **σ_ion 절대값에 규약 라벨이 없다** — Table S3 의 `0.553 / 0.558 mS cm⁻¹` 은 복셀 FV 값이라
   **협착(Holm) 항이 없다** (CL-81: `CONTACT_FREE` 상한, 코퍼스 실측 4.04×/6.69×).  같은 SI 에
   실험 `Rion`(Table S4)이 나란히 있으므로 *"협착 미포함 = 상한"* 을 병기해야 한다.
   ★ **상대비교는 안전하다** — SBE·DBE 가 같은 골격을 쓰므로 그 결손이 양쪽에 같은 크기로 걸린다.
   다만 그 **근거를 적어야** 한다 (CL-81: 무근거 "공통모드 상쇄" 금지).
3. ⚠ **porosity 규약 미기재** — Table S3 의 `7.86 / 7.37 %` 가 `ε_sphere` 인지 `ε_union` 인지
   안 적혀 있다.  둘은 이 계에서 **1.25 %p** 어긋난다.

### 확인 대기 2건
- Table S2 는 NCM 전자전도도를 한 값(`1.0×10⁻²`)으로 적는데 런의 상별 표에는
  `AM_S 0.01 / AM_P 0.005` 둘이 있다.  침대가 NCM 단일 반경(2.5 µm)이라 `AM_S` 하나만
  쓰였을 가능성이 크고 표의 값도 그것과 같다 — **침대 시딩에서 sid 를 확인**해 닫을 것.
- `SE coverage of AM 86.6 %` 의 정의(밴드·방식)가 안 적혀 있다.  우리 쪽에 Hertz/Tabor
  두 규약이 있고 값이 크게 갈린다.

### 표가 원장과 맞는 것 (확인 완료)
`σ_VGCF 100 → 78.5` 직경보존 재척도 ✓ (CL-47 의 "압축분말" 라벨도 표에 반영돼 있다) ·
`σ_ion(LPSCl) 3.0e-3` ✓ · `σ_ion(SDCP) 1.0e-3` ✓ · `σ_PTFE = 0` ✓ ·
DEM `E 1.35 / ν 0.3`, MPM `E 1.53 / ν 0.49 / σ_y 0.30` ✓ (전부 `Calibrated` 라벨) ·
PTFE centerline 차단 ✓ · 8 origin 반-복셀 factorial ✓.

## 8. 6 mAh Phase A — 남은 것과 비용 (2026-09-12 확정)

정본 = `docs/reviews/phase_a_6mah_order_prereg_20260907.md` (v2).  아래는 그 문서와 보유
데이터를 대조한 **현재 상태표**다.

| 단계 | 상태 |
|---|---|
| STEP2 (네 침대 W1–W4) | ✅ **완주** (2026-09-08, 원장 CL-85) |
| STEP3 `vox 0.15` primary 32팔 | ✅ **완주** (2026-09-12) — §5 |
| STEP3 `vox 0.20` primary 32팔 | ⬜ 미실행 |
| STEP3 `vox 0.25` primary 32팔 | ⬜ 미실행 |
| STEP3 QC exact replay 8팔 @0.15 | ⬜ 미실행 |
| Secondary (Lee 조성) 16팔 + STEP2 1런 | ⬜ 미실행 (⚠ `ORDER-*` 판정에 **관여 못 함**) |

### 판정까지 남은 것 = **72팔** (사전등록 §7 예산)
`0.20` 32팔 **12~14 h** · `0.25` 32팔 **6 h** · QC 8팔 **7~11 h** ⇒ **합 25~31 h** (단일 GPU 직렬).
★ 독립 산술로 교차확인: 침대 50.01 × 50.01 × 115.302 µm ⇒ 복셀 수가 `0.15 : 0.20 : 0.25`
= `85.8 M : 36.1 M : 18.4 M` (= 1 : 0.42 : 0.21) 이고, 실측 `0.15` 팔당 **≈44 분**을 복셀 수로
스케일하면 0.20 ≈ 18 분 · 0.25 ≈ 9 분 ⇒ 64팔 **≈ 15 h**.  등록 밴드(18~20 h)와 같은 자리다.

### ⚠ 64팔만 채워도 판정은 안 나온다
`phase_a_order_verdict.py` 의 `replay_gate` 는 **QC 가 하나도 없으면 거부**한다
(*"음성대조 없이 판정하지 않는다"*, R9 fail-open 수정분).  ⇒ **QC 8팔이 필수**다.

### ⚠ 거친 두 격자의 **의미는 이미 제한돼 있다** (사전등록 v2, Codex R9 Q2)
VGCF 는 항상 1-voxel tube 라 실제 Ø0.15 µm 대비 단면이 `h = .15/.20/.25` 에서
**1.27× / 2.26× / 3.54×** 로 바뀐다.  ⇒ 거친 64팔은 **grid convergence 를 증명하지 못하고**
*"접점이 지워져도 순서가 남는다"* 도 증명하지 못한다.  등록 문서 자신이 그것을
*"지정된 h-의존 디지털 규약의 stress test"* 로만 두라고 적었다.

### ✅ 확인 완료
- `--add-rng-per-phase` (조성 교락 차단, CL-71/77) 가 **킷 5개 run_mpm.sh 전부**에 있다.
- `vox 0.15` 32팔의 봉인 축이 전부 등록값과 일치 (§5) · `plate_rule = p2` 32/32.

### ⬜ 저자 판단이 필요한 갈래 (결과를 본 뒤의 선택이라 스스로 정하지 않는다)
**(A) 72팔을 채운다** — 등록된 그대로 완주해 `ORDER-*` 판정을 낸다.  25~31 h.
  계측기를 건드리지 않는다는 것이 이 길의 값이다.
**(B) 예산 조항을 발동한다** — 사전등록 §1 이 *"예산을 줄이면 primary 를 `h=0.15` 32팔로
  좁히고 거친 격자는 secondary 로 강등하는 것이 맞다"* 를 **런 전에** 적어 두었다.
  ⚠⚠ 그러나 지금 발동하면 **결과를 본 뒤에 설계를 좁히는 것**이 되고, 판정기의 `PREREG`
  상수를 고쳐야 한다 = **계측기를 결과 뒤에 수정**.  이 리포가 아홉 번 잡은 실패 모양이다.
  ⇒ 발동하려면 그 조항의 발동 조건이 *"예산"* 이었고 예산이 실제로 부족하다는 것을
  **수치로** 보여야 하는데, 위 실측(25~31 h)은 부족하다고 보기 어렵다.

⇒ **내 권고는 (A)** — 비용이 하룻밤 수준이고, (B) 는 얻는 것(시간 25~31 h)보다 잃는 것
(사전등록 계측기의 무결성)이 크다.

### ⚠ 사전등록 문서 자체의 내부 불일치 (닫아야 함)
v2 헤더 표는 `phase_a_plan.py` 를 **폐기(실행 거부)** 로 적고 *"정본은 킷 `run_mpm.sh`"* 라
하는데, 같은 문서 §8-1 은 그 도구를 **실행 도구 3종의 하나**로 적고 `--emit sh` 사용법까지
보여준다.  ⇒ 어느 쪽이 정본인지 확정하고 한쪽을 지워야 한다.

## 9. Codex 적대 리뷰 — 어댑터가 P1 8건으로 기각됐다 (2026-09-12 저녁)

판정 전문 = `docs/reviews/codex_verdict_phase_a_adapter_20260912.md` · 원장 `PA12-01~09`.
**결론: 현재 어댑터를 판정용 입력의 검증 경계로 신뢰할 수 없다.**
⚠ 런 계획은 안 바뀐다 — 기존 32팔이 잘못 계산됐다는 뜻이 아니고 (σ·vox·shift·라벨 대조
불일치 0 · protocol ID 32/32 · receipt 32/32 재현), 돌고 있는 h020·h025 도 그대로 간다.

### 가장 아픈 것 — 전부 **규율 ① 위반**
`conv_ok` · `receipt_match` · `protocol_ok` · `expected_origins_for` 가 `run_contract.py` 에
**이미 있는데** 부르지 않고 새로 짰다.  그 결과:
- 생산자가 잔차를 **`cg_resid`** 로 쓰는데 `CONV_KEYS` 에 없어 **잔차를 버렸다**.  게다가
  manifest 가 step3 의 `unconverged=True` 를 **덮어쓴다**.
- receipt 만 보고 payload 와 대조를 안 해 **p1 plate 팔도 통과**한다.
- origin 이 그 h 의 `{0,h/2}³` 인지 검사하지 않아 **잘못된 격자의 shift 를 8팔로 승인**하고,
  receipt 번호 둘만 바꾸면 `UNRESOLVED` → `ROBUST` 로 **뒤집힌다**.
- `role` 이 항상 `primary` 라 **QC 를 넣으면 primary 를 덮어쓴다** ⇒ ⛔ QC 변환은 수정 후에.
- 음성대조 ⑨ 가 **장식**이었다 (origin 검사를 퇴행시켜도 15/15).  셈도 틀렸다 —
  11개가 아니라 정상 5 + 음성대조 10.

### 내 오진 정정
요청서의 *"STEP2 에도 `add_rng_per_phase` 기록 코드가 없다"* 는 **틀렸다**.
`mpm3d_compaction.py:2156`(시딩)·`:3448`(metrics 기록)에 있다.  끊긴 곳은 **STEP3 manifest 로의
전달**이다.

### ★ 잔차 회수 — 러너 로그에 살아 있었다
내가 오후에 payload 4.6 GB 를 지워 `cg_resid` 가 사라졌는데, `~/pa/phaseA_h015.log` 에
`STEP3 σ_e_eff = …` 줄이 **정확히 32개** 남아 있었다.  전수 보존 =
`docs/data/phase_a_h015_arms/resid_from_runner_log_20260912.tsv`.
**잔차 전부 `[9.8e-09, 1.0e-08]`** · `n_dof` 32개 전부 유일하고 8개씩 네 무리
(46.9/48.1/49.3/50.5 M)로 갈려 VGCF 1<2<3<4 wt% 와 단조 대응 · 합 22.6 h · **42 분/팔**.
⛔ 그러나 **팔별 귀속은 안 된다** — 로그의 σ 는 봉인돼 있고 팔 JSON 에 dof 가 없다.
⇒ Codex 후속 질문 **Q7** 로 등록 (요청서 §7): batch-level 잔차를 이관 증서로 쓸 수 있나,
아니면 0.15 재실행(22.6 h)인가.  **내가 지운 탓을 내가 면제할 수 없어 스스로 정하지 않는다.**

### 재발 방지 (적용 중)
⛔ `~/pa/phaseA_h020` · `~/pa/phaseA_h025` 는 **변환·검증이 끝나기 전에 지우지 않는다.**
수정된 어댑터는 `cg_info`·`unconverged`·`cg_resid` 셋을 **손실 없이** 옮긴다.

## 10. ML 코퍼스 확장 — 정본은 `docs/lhs_design_dataset_20260818.md` §O 다 (2026-09-13)

⚠⚠ **내가 그 §O 를 안 읽고 답을 새로 만들었다** (규율 ① 의 문서판 위반).  두 군데 틀렸다:

1. ❌ *"LHS 는 1차 관측량이 이진 퍼콜이라 ML 코퍼스 행이 아니다"* — **lhs00 130 은 처음부터
   "AI 학습용 DEM 데이터셋" 으로 설계됐다.**  디스크립터가 `porosity · coverage · tortuosity`
   이고 타깃 이름에 규약까지 박혀 있다 (`coverage_*_hertz_pct` · `tortuosity_dijkstra_SE` ·
   `porosity_sphere_pct_RECORD_ONLY`).  크기는 `d_am`/`d_se` **직경** 규약으로 ML 설계층과
   맞춰 놨다.  퍼콜 이진은 **lhsx(08-29) 확장분의** 1차 관측량이지 lhs00 의 것이 아니다.
2. ❌ *"신규 68건부터 291 에 붙이자"* — §O-4 ⑤ 가 **정반대**를 등록해 뒀다:
   *"기존 코퍼스 291건과 합치는 것은 별개 사안 … **이 130행끼리는 자기일관하므로 단독으로
   먼저 학습하는 것이 안전하다**"*.

### §O 의 확정 사항 (재도출 금지 — 여기를 읽을 것)
- **같은 규약이면 추가는 안전하고 점수가 내려가는 게 정상**: σ_ion 57→92 (0.981→0.968) ·
  σ_th 82→90 (0.90→0.84–0.85) · σ_e 76→97 에서 **Stage 21 의 과적합이 드러나** 항을 뺀 22.5 가
  더 좋아졌다.  ⇒ *"점수가 내려가면 손해가 아니라 전에 부풀어 있었다"*.
- **진짜 위험은 같은 이름의 다른 양**: `cov_Hertz↔cov_physics`(91건 1.4배 과대) ·
  `d_am` 규약 충돌(중앙 **1.41배**·최대 **6.09배**) · `ps=0` 의 `d_am=0` 센티넬
  (**40/291 = 14 %**) · `ε_sphere↔ε_union`(1.251 %p) · `use_porosity_pct`(잔차 sd 가 ε sd 의 78 %).
- **고정 인자에 소수 행 = 지렛대**.  새 수준은 **블록(≥10~15행)** 으로.
  lhs00 이 고정한 것: `rve 50` · `loading 2 mAh/cm²` · `300 MPa` · `E_SE 1.35` · 첨가제 없음.

### lhs00 130 배치의 실체 (설계 CSV 전수)
블록 **bimodal 100 · mono_AM_S 15 · mono_AM_P 15** · 열 39개.
⚠ **`se_percolation = BELOW_phic` 가 44/130 (34 %)** — SE 가 퍼콜 문턱 아래라 σ_ion = 0 이 나온다.
⚠ `finite_size_flag` 가 **깨끗한 행은 62/130** 뿐 (`se_BELOW_phic` 22 · `n_am_p_low` 11 ·
`thin+n_am_p_low+se_BELOW_phic` 10 · `thin+n_am_p_low` 5 · `thin+n_am_p_CRIT` 5 …).
⇒ 플래그는 **거부가 아니라 라벨**이고 §3b 가 *"학습에 공변량으로 남겨 두면 모델이 흡수한다"*
고 적어 뒀다 — 그 규약을 따른다.

### 실행 상태 (2026-09-13 01:40)
- ibb: lhs00 **4개 잔여** (`004_r3` 남 1.5일 · `074/084_r2` 2.3일 · `029_r3` 4.8일, 압력
  0.225~0.266 / 목표 0.30).  나머지 lhsx 는 확장분이라 이 학습과 **별개**.
- 로컬 `DESKTOP-IK8J81H` `~/lhs_local` = `lhs00_100~129` (설계의 마지막 30):
  **완주 22 · 미실행 8**.  그 8개 중 **5개는 `BELOW_phic` 라 안 돌리는 게 맞고**(돌려도 σ_ion=0)
  **3개만 진짜 구멍**이다 (정본 `docs/reviews/lhs_local_gap_verdict_20260903.md`).
- ⇒ **지금 쓸 수 있는 완주분이 126/130 수준**이고 4개는 며칠 걸린다 ⇒ **기다릴 이유가 없다.**

### ⬜ 막힌 곳 — **수확기가 없다**
`scripts/lhs_*` 는 설계기(`lhs_design_dataset.py`)와 확장분 퍼콜용(`lhs_perc_extract/fit`)뿐이고,
**덤프 → 디스크립터(`porosity_sphere` · `coverage_*_hertz` · `tortuosity_dijkstra_SE`)** 수확기가
없다.  §P 는 제출까지만 닫았다.  ⇒ 291 이 거친 것과 **같은 파이프라인**
(`network_conductivity.py` 계열)을 130 덤프에 돌리는 것이 다음 작업이고, **GPU 가 아니라 CPU**다.

## 11. Phase A 잔여 72팔 — V100 대여로 이관 (2026-09-13)

### 왜 옮겼나
kgy 에서 `h020 → h025 → qc015` 직렬 러너를 걸었으나 **DFT 가 카드를 물어** 진행 불가:
`h020` **1/32** · `h025` **2/32** 만 내고 두 단계 모두 조기 종료했고(`--step3-require-gpu` 라
CPU 폴백 없음), `qc015` 는 여유 **941 MiB < 10,100** 으로 5분마다 두드리기만 했다.
⇒ 기다림이 아니라 **실패**였다.  러너 중지 후 전용 GPU 대여로 이관.

### 필요 사양 (실측 기반)
`≈210 B/dof` (LEAN=2 · σ_e 전용).  vox 0.15 의 **실측 10,032 MiB** 로 맞춘 계수다.
| 단계 | dof | 필요 VRAM |
|---|---|---|
| `0.25` | 10.3 M | ≈ 2.2 GB |
| `0.20` | **20.1 M** (로그 실측) | ≈ 4.2 GB |
| `0.15` (QC) | ~48 M | **≈ 10 GB** (실측) |
★ 이 계산은 **CG 반복 = 메모리 대역폭 병목**이다.  텐서코어·FP16 을 전혀 안 쓰므로
*"싸고 대역폭 높은 구형 카드"* 가 신형보다 유리하다 (A100/H100 은 과잉).

### 대여 인스턴스 (runyour.ai)
`Tesla V100-PCIE-32GB` · **32,768 MiB** · RAM **125 GB** · 디스크 **925 GB 여유** · **32 코어** ·
컨테이너 `3f2c2428df7f`.  ⇒ 전 단계가 여유롭게 돌고, **CPU 32 코어**라 LHS 디스크립터 수확
(§10 의 막힌 곳)도 여기서 병행 가능하다.

### 세팅 (전부 기존 도구, 새로 짠 것 없음)
정본 = `docs/server_bootstrap_runbook.md`.
- 로컬: `scripts/setup_v100_local.sh <pem> ubuntu@machine.runyour.ai 22` → `ssh v100` 완성.
  ⚠ 그 스크립트의 **마지막 안내문은 낡았다** — `setup_v100.sh` 를 가리키는데 그것은
  `se_curve` 배치 전용 레이아웃 복원기다.  우리에게 맞는 것은 런북 §② 의
  **`setup_gpu_server.sh`** (범용 환경 재건).  둘은 다른 일을 한다.
- 서버: `setup_gpu_server.sh` → **7/7 통과** (taichi 1.7.4 · **cupy GPU sparse CG OK info=0**).
- 코드 고정: `git checkout 70b9e37a` (**detached HEAD 가 맞다**) — 0.15 32팔이 그 `code_sha` 로
  돌았고 **같은 캠페인에서 코드가 섞이면 안 된다**.

### ⚠ 이번에 밟은 지뢰 — 긴 설치를 SSH **포그라운드**에서 돌렸다
`[4/7] python 패키지` 중 `Connection closed by remote host` 로 죽고 이어서
`Permission denied (publickey)` 가 났다.  **원인은 자원이 아니었다** — 재접속해 보니
RAM 125 GB · 디스크 925 GB 여유 · 컨테이너 ID 동일(재시작 없음)이었다.  **SSH 가 끊긴 것**이다.
⇒ 런북이 런에 `setsid nohup` 을 쓰는 것과 **같은 이유로 설치도 떼어내야** 한다:
`setsid nohup bash -c 'curl … | bash' > ~/setup.log 2>&1 &`.

### ⬜ 남은 것 = 침대 전송
`~/pa/kits03` = 4킷 × ~2.35 GB = **9.4 GB** · 부분결과 `h020` 144 MB + `h025` 290 MB.
⚠ **pem 이 kgy 에 없다** ⇒ 경로 셋: ⓐ pem 을 kgy 로 옮겨 **직송** ⓑ `DESKTOP-IK8J81H` 경유
(두 번 전송) ⓒ **필요 파일만 추려 직송** — 러너는 매니페스트 기준 **6개**
(`scaffold`·`se`·`phase`·`fibre`·`fibre_dia`·`metrics_json`)만 읽으므로 ⓒ 가 가장 싸다.
⇒ 킷 내부 파일 크기 실측 대기 중.

---

## 12. Codex 적대 리뷰 ② — LHS 130 디스크립터 추출 경로가 **HOLD** (2026-09-13)

정본 = `docs/reviews/codex_verdict_lhs_descriptors_20260913.md` (전문 + 내 독립 재현 부록).
원장 등재 = `DESC-01`~`DESC-09` + `SELF-21`.
기준 스냅샷 `be79f0827ea253ffb027b584e37067b60b03aa7c`.

**판정: 현재 웹앱 출력물을 그대로 일곱 열에 매핑해 학습 코퍼스로 확정하는 것은 HOLD.**
설계 좌표·고정 인자를 재심한 것은 **아니다** — 계측기 문제다.

### 세 P1 (전부 이 리포에서 독립 재현 완료)

| ID | 무너지는 보장 |
|---|---|
| `DESC-01` | τ 가 실패하면 **기하량** `phi_se`·`phi_am` 까지 사라진다 (공극률은 저장되는데도).  ⇒ 완전행 필터 = 연결성 약한 침대가 구조 타깃째 탈락 = **결측이 물리와 상관** |
| `DESC-02` | 유한 τ ≠ 관통 보장 (`percolation_pct=0` 인데 τ=1) · τ=None ≠ 미퍼콜 보장 (200쌍 예산을 무경로 쌍이 먹는다) |
| `DESC-03` | Physics 피복률이 **확대 DEM 길이 ÷ 미확대 5 nm** 를 쓴다 ⇒ **cap 선택이 뒤집힌다** |

`DESC-03` 은 수치까지 재현했다 — 반지름 0.5 µm · δ/R\* = 0.05 를 실제 함수에 넣으면
binding cap `tabor` → `volume`, 비 **2.502607 → 1.229167** (비의 비 **2.036019**),
즉 Physics 피복률 **0.782065 % → 0.384115 %** (Hertz 는 **0.312500 % 불변**).
⇒ 내가 요청서에 쓴 *"큰 Δ 는 정의 차이뿐"* 은 **반증**.
⚠ 그러나 **Hertz 가 불변이므로 등록 타깃이 Hertz 전용인 한 이 오류는 일곱 열로 전이되지 않는다** —
판정문이 그은 경계를 넘겨 읽지 말 것.  화면 Δ 세 수치의 원인이 이것이라고도 **단정하지 않았다**
(원자료 미제공).

### 내 요청서가 틀린 곳 세 가지

1. **호출 경로를 잘못 시작했다** — 피복률·φ·τ 의 주 생산자는 전도도 솔버가 아니라
   `app.py:3271 → analyze_contacts_bimodal.py → analyze_contacts.py → dem_analysis_core.run_full_analysis` 다.
2. ★ **철회된 권고를 인용했다** (`SELF-21`) — 로컬 미실행 8개 중 *"5개 제외"* 는
   `docs/reviews/lhs_local_gap_verdict_20260903.md:68` 에서 저자가 이미
   **"8개 전부 돌린다.  게이트를 걸지 않는다"** 로 정정해 놓았다.  같은 파일의 68 행이다.
   그 이유까지 거기 적혀 있었다 — `se_percolation` 은 `phi_se_est`(런 **전** 추정) 파생이라
   그것으로 실행을 거르면 *"검사기가 대상의 자기 신고를 읽게 하지 마라"* 위반.
   ⇒ 규율 ④ 가 **판정문 안에서** 재현됐다.  이번엔 낡은 정본이 아니라 **읽다 만 정본**이다.
3. `phi_se`/`phi_am` 상시 생산 · 전체 Hertz 피복률 키 상시 존재 — 둘 다 REFUTED.

### 숫자 한 줄 정정

**44/130 은 `phi_se_est` 기반 사전 추정 BELOW flag (33.846 %)** 이지 *"44 % 가 실측 σ_ion = 0"*
이 아니다.  일곱 측정 열은 **130 행 전부 빈칸**이므로 σ_ion 열 자체가 없다.

### 미실행 8개는 무작위 결측이 아니다 (`DESC-09`)

8개 **전부 mono_AM_S** 이고, **mono_AM_S × d_SE = 2 µm 다섯 점(105·108·109·110·111)이 전부**
그 목록에 있다 — 남은 mono_AM_S 에 그 수준이 **0 개**.  공변량·가중치로 **관측이 전혀 없는 구간**은
못 살린다.  ⇒ 교정된 완료분으로 **탐색 학습은 가능**하나 130-domain 최종 성능·순위로 보고하면 안 된다.

### 다음 (수확 전 최소 계약 6개 — 판정문 §7)

① 일곱 별칭 → 원필드/계산식/단위/통계량 정확 매핑 (전체 Hertz 는 **실측 입자수**로 집계)
② φ 는 τ·전도도 성공과 **무관하게** 기하에서
③ τ 의 관통성·경계·표본·절단·실패 사유 명시
④ Hertz 보존 + Physics helper 단위 일관성 검증
⑤ frozen130 ID ↔ 최종 pressure/timestep · raw hash · phase map · 상 개수 · target 별 상태
⑥ **130 단독 코퍼스 먼저** — 291 병합은 `d_am`·MPM/DEM 규약 정리 후 별도 판단
  (§O-4 ⑤ 의 *"이 130행끼리는 자기일관하므로 단독으로 먼저 학습"* 과 **같은 결론**이다)

⛔ **수확기를 짜기 전에 ①~⑥ 을 도구에 고정한다.**  ⚠ 규율 ① — 그 도구를 새로 짜기 전에
`scripts/` 에 이미 있는 것부터 본다 (어댑터에서 `run_contract.py` 를 못 보고 새로 짠 것이
`PA12-01`~`05` 의 절반이었다).

---

## 13. V100 실행 결정 — **QC 도 같은 V100 에서** (2026-09-13, 저자 판단 · 결과 **보기 전**)

### 발견 — 이 V100 은 **공유 GPU** 다
`nvidia-smi --query-compute-apps` 가 우리 네임스페이스에 **없는 PID 2341825** 가
**11,040 MiB** 를 쥔 채 utilization **47 %** · 74 °C 로 돌고 있음을 보여준다
(컨테이너 안의 프로세스 표는 *"No running processes found"*).  ⇒ **다른 테넌트와 물리 GPU 공유**.
- 가용 VRAM = 32 GB 가 아니라 **≈21.7 GB** 이고 이웃이 늘리면 더 준다.
- SM 을 나눠 쓰므로 kgy 실측 **42 분/팔** 기준의 25~31 h 추정은 **낙관**이다.
- 메모리 여유(dof 비 환산): `0.20` ≈ 4.3 GB · `0.25` ≈ 2.2 GB · **QC `0.15` ≈ 10.0 GB**
  (실측 10,032 MiB) ⇒ QC 가 가장 빠듯하다 (11 + 10 = 21 GB).

### 갈래 — QC 만 구조적으로 **교차-기계**가 된다
h015 primary 32팔은 **kgy GPU** 에서 돌았다.  사전등록 §3 의 음성대조는 *"동일 materialized
STEP2 배열 · 동일 규약을 **다른 출력 경로**에서 재계산"* 인데, QC 를 V100 에서 돌리면 거기에
**다른 하드웨어**가 얹힌다 — 등록된 변주가 아니다.
⚠ 거친 64팔(`0.20`·`0.25`)은 **무관**하다: 격자 안에서 4 wt% × 8 origin 이 전부 같은 기계라
`ORDER-*` 추정량은 오염되지 않는다.

### 결정 = **(가) QC 도 V100**
저자 선택.  ⛔ **그 대가를 여기 명시한다**: QC 8팔의 `d` 가 `δ_num = 0.04 %` 를 넘으면
사전등록 §2 ⛔ 대로 **문턱을 넓히지 않고 전체 HOLD** 이고, 그때 *"기계가 달라서"* 는
**사후 변명으로 쓸 수 없다**.  이 선택을 방어 가능하게 만드는 유일한 근거는
**결과를 보기 전에 정했다**는 것이고, 그 사실을 이 문단이 기록한다.
★ 통과하면 얻는 것도 있다 — 같은 δ_num 안에서의 **기계 독립성**이 덤으로 보인다.
(기각된 대안 (나) = QC 를 kgy 에서 = 쌍둥이와 같은 기계.  DFT 일정이 kgy 를 막아 탈락.)

### 실행 상태
- 코드 `70b9e37a3` 고정 (h015 32팔의 `code_sha` 와 동일) · 트리 깨끗.
- 침대 4킷 전송 완료 (6.9 G) — `se_dump`·`se_dump_eps`·`phase`·`fibre`·`fibre_dia`·
  `mpm_metrics.json` 4/4 킷 전수 확인, `latest_run` 심링크 재생성.
  ⚠ rsync 가 심링크를 안 옮겨 다시 걸었는데, 첫 시도의 글롭 `run_*` 이 **`run_mpm.sh` 를
  물어** 심링크가 스크립트를 가리켰다 (`run_*/` 로 수정).
- 부분결과 `phaseA_h020` 144 M · `phaseA_h025` 290 M 보존 ⛔ (§9 재발방지).
- 러너 `~/pa/*.sh` 11개를 kgy 에서 회수 — **리포에 없는 임시 스크립트**라 재작성하지 않고
  h020·h025 를 실제로 만든 그것을 그대로 옮겼다.

### ★ 첫 런이 거부됐다 — 그리고 **그것이 옳았다** (2026-09-13 03:05)

러너의 kgy 경로 3개를 고쳐 걸었더니(`~/dem-venv/bin/activate` → `activate_dem.sh` ·
`/home/kgy/` → `/home/ubuntu/`) `h020`·`h025` 가 **같은 초에** `rc=0 · 팔 1 / 팔 2` 로 끝났다.
처음엔 fail-open 으로 읽었는데 로그를 보니 **fail-closed 였다**:

```
[p2] ABORT — 이 디렉터리의 영수증이 지금 설정과 다르다: …/phaseA_h020/run_receipt.json
     같은 디렉터리에 다른 설정의 팔을 섞을 수 없다.  OUTDIR 을 갈라 쓸 것.
```

| | `code_sha` | |
|---|---|---|
| h015 primary 32팔 | **`70b9e37a`** | 캠페인 핀 (커밋된 `run_receipt.json` 로 확인) |
| kgy h020 1팔 · h025 2팔 | **`48d145f1`** | 다른 커밋 |
| 지금 V100 | `70b9e37a3` | 핀 복원 ✓ |

나머지 봉인 축(`bridge_um` · `fibre_stamp` · `ptfe_stamp` · `origins` · `periodic_xy` ·
`sdcp_*` · `vox`)은 **전부 동일**하고 `code_sha`(와 그 파생 `receipt_digest`)만 다르다.

★ **두 sha 의 차이를 실측했다**: `48d145f1` 은 `70b9e37a` 의 **후손**이고 델타는
**`docs/data/phase_a_h015_arms/` 35개 파일뿐** = 내가 09-12 저녁에 커밋한 **이 캠페인 자신의
출력 데이터**다.  `git diff --stat 48d145f1 70b9e37a -- scripts/ webapp/` 가 **빈 출력** ⇒
**실행 코드는 바이트 동일**하다.  kgy 작업트리가 그 데이터 커밋 위에 있었을 뿐이다.

⛔ **그럼에도 가드를 풀지 않는다.**  코드가 같다는 것은 *확인된 사실*이지 **발화한 검사를 그
다음에 느슨하게 할 근거가 아니다** — 이 리포가 아홉 번 잡은 실패 모양이 정확히 그것이다.
대가도 거의 없다: 재실행 **3팔 ≈ 36분** (`0.20` 18분/팔 · `0.25` 9분/팔) 대 64팔 15~20 h.
⇒ 3팔은 `phaseA_h0{20,25}_sha48d145f1_kgy` 로 **옮겨 보존**하고(지우지 않는다 — §9)
두 격자를 핀에서 32팔씩 새로 돈다.

★ `qc015` 는 디렉터리가 비어 있어 새 영수증을 쓰고 **정상 진행 중**이다 (8,130 MiB · 23 %).
h015 primary 와 **같은 `70b9e37a`** 라 §13 의 (가) 교차-기계 말고는 조건이 같다.

⚠ 러너 자신의 결함도 남는다 — **ABORT 를 `rc=0` 으로 삼킨다**.  `팔 N` 이 32 미만인데
`종료 rc=0` 이 찍히면 watch 만 보는 사람은 완주로 읽는다.  리포 밖 임시 스크립트라 원장에
올리지는 않되, 재사용 전에 **stage rc 를 전파**하도록 고칠 것.

### ★★ 두 번째 런도 0팔 — 원인은 **환경 하나** 였다 (`SELF-22`, 2026-09-13 03:12)

3팔을 옆으로 치우고 다시 걸었더니 이번엔 **세 단계 전부** `팔 0` 이었다.  로그 전문에서:

```
[p2] 규율 검사 통과
ABORT — 미정의 이름 발견.
미정의 이름 검사 (ast(최소)) — 파일 5
  ✗ …/additives.py:744: undefined name 'curl' in _grow
  ✗ …/additives.py:600: undefined name 'in_am' in _filt
  ✗ …/viz_mpm_continuum.py:136: undefined name 'film' in voxelize
✗ 114 건
```

전부 **중첩 함수의 클로저 변수**다.  `sdcp_gain_vox015_8arm.sh:584` 의 미정의-이름 게이트가
부르는 `check_undefined_names.py` 는 **pyflakes 가 있으면 그것을, 없으면 AST 최소 검사**로
떨어지는데(`:8` · `:89-95`), 최소 검사는 클로저를 못 본다.

★ **재현으로 확정**: 이 컨테이너(pyflakes 없음)에서 같은 5파일을 돌리면 **정확히 114 건** =
V100 과 같은 수 ⇒ **코드 결함이 아니라 백엔드 차이**다.  h015 32팔은 kgy 의 pyflakes 로
게이트를 통과했으므로, V100 에 pyflakes 를 넣는 것은 **게이트 약화가 아니라 동일화**다.
실측: 넣으니 `3.4.0` · **`✓ 없음`** (114 → 0).

**리포의 구멍**: 런북 §② 가 가리키는 `setup_gpu_server.sh` 에 pyflakes 가 **없고**
`setup_gpu_host.sh:75` 에는 **있다**.  ⇒ 커밋 `dcc723e1e` 로 [4/7] 패키지에 추가하고
[7/7] 검증에 import 한 줄을 넣었다 (이유를 주석으로 박음).  원장 `SELF-22`.

⚠ 남은 결함 둘 (이번엔 안 고침):
1. 러너가 **stage ABORT 를 `rc=0` 으로 삼킨다** — `팔 0` 인데 `종료 rc=0` 이라 watch 만 보면
   완주로 읽힌다.  리포 밖 임시 스크립트라 원장엔 안 올렸지만 재사용 전에 rc 를 전파할 것.
2. `check_undefined_names.py` 가 **최소-AST 로 떨어졌다는 사실을 호출자에게 구별 가능한
   형태로 알리지 않는다** — 지금은 오탐 114건과 진짜 NameError 가 같은 exit 로 나온다.
   게이트가 "백엔드=pyflakes" 를 **요구**하게 하는 것이 옳다.

---

## 14. Codex 적대 리뷰 ③ — DEM 스택 **L1 판정 = HOLD** (2026-09-13)

정본 = `docs/reviews/codex_verdict_dem_stack_L1_20260913.md` (전문 + 내 독립 재현 부록).
원장 = `L1-01`~`L1-08` + `SELF-23`.  기준 `d4bae885a`.

**판정: 단위 오류를 고쳐도 현재 Physics 면적을 "검증된 물리 접촉면적" 으로 승인할 수 없다.**
⚠ *"DEM-native 기하량까지 모두 폐기하거나 130개 DEM 을 다시 돌리라는 판정은 아니다."*
⇒ **기하 장부는 보존하고 그 위에 얹은 소성·수송 모델을 분리**하는 것이 처방이다.
⚠ 정식 판정은 **L1 까지**다 — L2~L5 는 통과로 세지 않는다.

### 단위를 고쳐도 남는 새 P1 셋 (전부 우리 트리에서 재현)

| ID | 재현값 |
|---|---|
| `L1-01` 하한 > 상한인데 정상 반환 | SI · r 0.5 µm · δ/R\*=**0.01**(얕다) → `binding=liggghts`, A/A_tabor **1.784757** · A/A_volume **8.016722**, `L>U` **True** |
| `L1-02` `V_overlap` ≠ 참 lens | δ 0.0125 µm → **0.493724배** · δ 0.95 µm → **−0.094509579 µm³ (음수)** 인데 양의 면적 반환 |
| `L1-03` 상 쌍 인자가 없다 | 시그니처에 phase/pair/E\*/H **없음**; 전역 `E_STAR_AM_SE 2.2414943e10` · `H_REAL_SE 8.5e8` 을 AM–AM·SE–SE 에도 적용 (reduced modulus 는 74.67 / 22.41 / 13.19 GPa 로 갈린다) |

`L1-05` 도 소수 자리까지 재현됐다 — 사면체 4구에서 `ε_union = **110.1472 %**`(>100), union
부피 **−6.49420**, 참 ε ≤ 93.4550 %.  ★ **2구 대조는 정확한 lens 와 일치**하므로 결함이
**삼중 이상 겹침의 inclusion–exclusion 부재**로 특정된다.

### ★ 내 결론 하나가 좁혀졌다 (`SELF-23`)

판정문은 내 `DESC-03` 반례를 *"그 입력에서 **유효**하며 철회할 이유가 없다"* 고 했다.
좁혀진 것은 그 **다음 문장**이다 — *"교정하면 binding 이 대부분 volume 으로 넘어간다"* 는
**미증명**이다.  내 반례가 `A_LIGG = A_Hertz` 를 놓은 입력이어서고, 공식 LIGGGHTS 기하면적
(비 **1.9875**)을 넣으면 최종 선택은 **LIGG floor** 이며 coverage 는 0.78206456 % →
**0.62109375 %** 다 (내 toy 의 0.38411458 % 가 아니다).
⇒ 교훈: **합성 반례의 입력 가정이 결론의 적용 범위를 정한다.**

### ⛔ 두 번째 정정 — *"Hertz 전용 타깃이면 안전"* 이 틀렸다 (`L1-07`)

feature 는 Hertz-명명이어도 **타깃 y 가 Physics Stage-E 우선**이다 ⇒ **이온 T1 은 정상 행도
y 가 변할 수 있고**, **열 T1 은 X·y 둘 다** 바뀐다 (ridge 가 면적 선형 열을 표준화하지 않아
단순 단위 재표현만으로도 같은 α 에서 적합이 달라진다).  전자 22.5 는 보통 불변이나
**Physics fallback 행**은 다르다.  Codex 실행: `(ionic y, cov, electronic y)` =
**(3, 0.1, 11) → (6, 0.1, 11)**, electronic fallback **12 → 24**.

⚠⚠ **재적합은 별도 사전등록 사안**이다.  기존 데이터를 이미 봤으므로 최초 사전등록이 아니라
**"오류 교정 후속 프로토콜"** 로 표시하고, **LOCKED 지수·EXCL·φ_c·endpoints/drop set·검증
분할을 유지**한 채 ① 기존 계수를 새 데이터에 그대로 평가 → ② 등록 LIVE 계수만 재적합 을
**따로** 보고한다.  ⛔ 유리한 모델/지수/문턱 재선택을 버그 수정에 끼워 넣지 않는다.
⚠ 세대 순서 강제: `coverage → raw Physics network → Stage-E → 생산식 데이터셋`.
*"Stage-E 만 돌려 값이 같았다"* 는 불변성 증명이 아니다.

### ⚠ 섞으면 안 되는 것

합성망에서 coverage **−73.2 %** 인데 최종 Stage-E σ 는 **+259.8390 %** — **부호가 반대**다
(`network_conductivity.py:396` 의 `1/[2a(1−a/r)^1.5]` 비단조 + 1.1×baseline 초과 시
weighted-factor fallback).  ⇒ *"σ 는 coverage 의 제곱근만큼"* 환산은 **반증**.
⛔ 이 비율을 화면 Δ 나 실코퍼스에 **적용 금지** — 전부 합성이고 발생률 미측정이다.

### 이번에 한 것 / 안 한 것

- ✅ `L1-04` 처치: 새 수확기 출력에 **`area_channel`** 을 박았다 (`a9db0c15d`) — 등록 별칭은
  **안 바꿨다** (판정문: *"기존 키와 frozen feature 를 세대 표시 없이 바꾸면 안 된다"*).
- ⬜ `L1-01`(cap_conflict 기록) · `L1-02`(lens 정의) · `L1-03`(pair 지원 범위) — **미착수**.
  ⚠ 셋 다 *"어느 쪽을 의도했는가" 를 먼저 정해야* 해서 코드만으로 못 닫는다.
- ⬜ `L1-05`(삼중 겹침) · `L1-06`(B3 라벨) · `L1-08`(5 nm 앵커) — 미착수.
- ⬜ **L2~L5 판정 대기.**  `Q7`(Phase A batch-level 잔차)도 별건으로 남아 있다.

---

## 15. Codex 적대 리뷰 ④ — DEM 스택 **L2 판정 = HOLD** (2026-09-13)

정본 = `docs/reviews/codex_verdict_dem_stack_L2_20260913.md`.  원장 `L2-01`~`L2-10` + `SELF-24`.
기준 `570d04039`.  **P1 6그룹 · P2 4건.**

### ★★ 가장 큰 것 — **협착저항의 ψ 가 분모에 있다** (`L2-01`)

```python
# network_conductivity.py:398
R_constriction = 1.0 / (sigma * k_weight * 2 * a_eff * psi)      # = R_H / ψ
```
문헌형은 `R_c = ψ/(2 k a)` = **`ψ · R_H`** (Yovanovich 1982 p.86 식 1–3, 2005 리뷰 p.187 식 7–9;
둘 다 유효범위 **0 < a/b ≤ 0.3** 명시).  ⇒ **역수 적용**이고 비는 `(1−s)^−3` 이다.

**이 리포에서 자리까지 재현**:

| a/r | R_code | ψ·R_H | 비 = (1−s)^−3 |
|---:|---:|---:|---:|
| 0.1 | 5.85606974 | 4.26907484 | 1.371742 |
| 0.2 | 3.49385621 | 1.78885438 | 1.953125 |
| 0.3 | 2.84578240 | 0.97610336 | **2.915452** |

⚠ **깊은 겹침을 요구하지 않는다** — `a/r = 0.1` 에서 이미 1.37배다.
그리고 비단조·불연속도 재현됐다: 최소점 **s = 0.4**, `s=0.8` 에서 R = 6.98771243(면적↑인데
저항↑), `ψ ≤ 1e-4` 절단점 **s\* = 0.99784556531** 에서 **5010.76059484 → 0**.
⇒ 코드 주석의 *"Full contact limit: spreading vanishes"* 는 극한이 아니라 **절벽**이다.

⛔ **처방이 ψ 위치 수정만이 아니다.**  `:323` 이 `a = √(A/π)` 로 A 를 **원판 면적**으로 읽는데
Physics 의 `2πR_min²` **표면 cap** 을 넣으면 `a = √2·r` 가 되고 `:394` 에서 다시 r 로 잘린다
⇒ **구 표면 피복과 평면 전류 원판을 같은 양으로 취급**한다.
★ **이것이 `L1-01/02/03` 을 안 고치고 기다린 것의 답이다** — 판정문이 *"L2 에 넘길 면적 A 가
무엇인가를 먼저 정하라"* 고 했고, 두 층이 같은 결정에 걸려 있다.

### 나머지 P1 다섯 (소스 위치는 확인, 실행 재현은 미실시)

| ID | 무너지는 보장 |
|---|---|
| `L2-02` | **정상 미퍼콜(valid_null)을 양의 fallback 으로 복원** — 실제 비관통 망에서 σ_e = 0.0832670335 이 나오고 `trustworthy_overall = true` |
| `L2-03` | grain factor = 1 이면 **파괴로 3접촉이 끊겨도 solver 를 0회 호출** — σ 가 raw 그대로인데 UI 는 *"Direct solver"* 를 낸다 |
| `L2-04` | **벽에 안 닿는 사슬을 관통으로** — 전극을 내부 15/85 % 로 옮기는데 정규화 길이는 plate_z 그대로 |
| `L2-05` | **전류 0 인 막다른 가지** 하나로 σ **+3.995844 %** (전극 g 가 전 간선의 sum/max) |
| `L2-06` | 수치 사고 → `valid_null` · **thermal NaN → `computed`** (이온·전자는 막는데 열만 안 막는다) |

### ⛔ 우리 정본이 하나 틀렸다 (`SELF-24`) — CLAUDE.md 를 같은 커밋에서 고쳤다

CL-81 절의 *"문헌 삼각측량: kim2025 EIS 의 GB-only 3.75× 가 우리 4.04 와 8 % 안에서 만난다"* 를
**철회**했다.  `R_brug_over_full` 은 **CONTACT_FREE/FULL** 이고 CF 는 **같은 접촉망의 bulk-only
가지**라 그 배수는 **모델 내부 민감도**이지 실험 대비 오차가 아니다 (같은 4구에서
CF/FULL **3.6477** 인데 Bruggeman/FULL **0.2771** — 부등호 방향이 반대다).
★ **무너지지 않은 것**: 복셀 FV 가 CF 가지 위에 있고 Holm 항이 정확히 0 이라는 **구조적 결론**은
그대로다 — L2 가 CF = bulk-only 임을 오히려 확인했다.

### 화면 문구 하나 더 (`L2-08`)

*"협착이 전체 저항의 45 %"* 는 **간선별 비율의 비가중 평균**이다.  같은 망의 실제 전력 몫
`ΣI²R_c/ΣI²R_total` 은 **8.1818 %** — **5.5배** 차이다 (이 리포에서 재현).

### 판정문이 지시한 수정 순서 (우리가 따를 것)

1. **L1↔L2 면적/수송 접촉 계약을 먼저 정한다** (원판 / 막 / 표면 구분 → 접촉 연산자 선택)
2. boundary / state / 0R 계약 복구 + 회귀
3. Stage-E 의 **계산과 추정을 분리** (동일 입력일 때만 skip · 정상 미퍼콜 양수 복원 금지)
4. consumer 를 **명시적 선택**으로 (양수 우선순위 getter 에 zero/실패/estimate 를 섞지 않기)
5. 대표 raw case 고정 → **수정 전후 재추출** (기존 산출물은 지우지 말고 **세대를 나눈다**)
6. **그 다음에야** 별도 사전등록된 재적합 검토

⚠ 이번 판정도 **LIVE 계수 재적합도, LOCKED 지수·EXCL·φ_c 변경도 승인하지 않았다.**

### L3 로 넘어간 단서 하나

같은 레코드에 `Physics Stage-E = 0` · `raw Physics = 0.7` 을 넣으면 **grade 이온 getter 는 0**,
**생산 target getter 는 0.7** 을 반환한다 ⇒ 정상 zero 가 생산식에서 **다른 값으로 바뀐다**.

---

## 16. Codex 적대 리뷰 ⑤ — DEM 스택 **L3 판정 = HOLD** (2026-09-13)

정본 = `docs/reviews/codex_verdict_dem_stack_L3_20260913.md`.  원장 `L3-01`~`L3-12` + `SELF-25`.
기준 `f28d7a53c`.  **P1 3건 · P2 9건.**

### P1 셋

1. **`L3-01` — 정상 Stage-E 0 이 raw 양수로 대체된다** (이온·전자·열 전부).  우선순위가
   **첫 양수**이고 **source 를 안 읽는다**.  ⛔ **L2 에서 0 을 보존하게 고쳐도 이 소비자가 다시
   양수로 바꾼다** — L2 수정만으로 안 닫힌다.
   ⚠ 전자는 거부 조건이 **두 source 의 AND** 라 *선택하지 않은 분기가 선택한 값을 보증*한다.
2. **`L3-02` — 같은 T1 인데 gate 가 둘이다.**  생산 네 곳(`G:4585`·`4694`·`4868`·`5185`)이
   `_sat_baselog` 를 **인자 6개 · AM 반경 없이** 부르고 요인분해 `G:2314` 만
   `_sat_g_smooth(p, r_AM_S, r_AM_P)` 로 넘긴다 — **이 리포에서 전수 확인**.
   AM 반경만 (2,4)→(4,10) µm 로 바꿔도 생산 base 는 **비트 단위 불변**, size-aware 는 1.684685배.
3. **`L3-03` — cov_Hertz 가 없거나 0 이면 Physics 로 바뀐다** (base √3 = 1.732051배).
   ⇒ **내가 L1 회신에 쓴 "이온은 Hertz 라 영향 없다" 가 세 번째로 좁혀졌다**
   (`SELF-23` → `L1-07` → 여기).

### 우리 정본이 또 하나 틀렸다 (`L3-06`, 커밋 `ea11cdaf7`)

`_EXCLUDED_NAMES_EL` 은 **23 이 아니라 25개**다.  CLAUDE.md 를 같은 날 고쳤다 (규율 ④).
⚠ **이름 25개 ≠ 실제 탈락 행 수**.

### `L3-11` — 설명 세대가 **셋**이다 (판정문은 둘까지 봤다)

| 상수 | 실제 | 파일 안의 설명 |
|---|---|---|
| EXCL | **25** | 23 |
| 열 특징 | **14** | `G:6703` **18** · `G:6911` **16** |
| 열 Ridge α | **0.05** | `G:6911`·`G:7039`·`G:6694` 전부 **0.1** |
| 전자 항 | **8 LIVE** | 제목 `G:2855`/`:2858` 에 드롭된 4항이 살아 있어 **12항 모델**로 읽힌다 |
| 전자 CSV 열 | 22.5 예측값 | 열 이름은 **`Stage_15_form`** |

⇒ **그림·CSV·report 를 Methods 로 전사하면 계산하지 않은 모델을 보고하게 된다.**

### ★ CONFIRMED 된 것도 있다 (전부 폐기가 아니다)

전자 **8 LIVE + 2 LOCKED · 4 drop** · 열 **14특징 · Ridge α=0.05** · EXCL 공유와 실제 제외
(EXCL 행 target 에 +100 을 줘도 계수 변화 **0**) · **고정 X/y 의 coefficient LOO 계산은 정확**
(독립 hat-matrix 와 소수 15자리 일치) · 전자 **φ_AM > 0.30 도메인 가드 작동**.
⇒ *"선형대수 엔진 자체의 전면 재작성 근거는 없다"* — 문제는 **분산된 getter·feature·설명
생성부**다.

### ⚠ 내 검증이 한 번 틀렸다 (`SELF-25`)

정규식으로 상수를 세다가 EXCL 을 **0개**, 열 특징을 **16개**로 읽고 *"판정문 숫자가 우리 트리와
다르다"* 고 쓸 뻔했다.  괄호 균형으로 다시 세니 **25·14** 로 판정문과 정확히 같다.
⇒ **엉성한 grep 으로 판정문을 반박하지 않는다.**  `SELF-02~05` 와 같은 부류인데 이번엔
**내가 대조하는 쪽**이었다.

### 남은 것

`L4` 전체 · `L5` 전체 · LHS130 수확기 · Phase A `Q7` — 전부 미판정.
⚠ 판정문: *"이번 L3 HOLD 는 L1/L2 의 미해결 물리 문제를 해제하지 않는다."*
