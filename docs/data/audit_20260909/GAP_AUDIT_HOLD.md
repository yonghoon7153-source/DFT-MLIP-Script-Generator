# 갭 감사 (91 버킷 · 798 파일) — **다음 주로 보류** (2026-09-09)

> 사용자 판단: *"토큰이 없다 이건 일단 hold 하고 나중에 한번에 하자 더 업그레이드 해서 /
> litdb 도 사실 건들 필요없어 / 근데 보면 좋으니까 다음주에 하자"*

---

## 지금까지 끝난 것 (버리지 않는다)

`gap_partial_buckets.json` — **8 버킷 · 96 파일** 인벤토리 완료.
전부 `assigned == inventoried` (계약대로 배정 파일을 하나도 안 빼먹었다).

| 버킷 | 파일 | 상태 요약 |
|---|---|---|
| `(root)` | 17 | live 4 · superseded **10** · historical 1 · data 2 |
| `.claude` | 9 | 전부 live |
| `.github` | 1 | live |
| `config` | 1 | live |
| `dem_scripts#1` | 22 | live 18 · dead 1 · superseded 1 · historical 1 · data 1 |
| `dem_scripts#2` | 2 | live 2 |
| `docs#1` · `docs#2` | 44 | 전부 data |

---

## 남은 것

**83 버킷 · 702 파일.**  버킷 정본은 같은 폴더 `buckets.json` (94 버킷 전체).
남은 목록은 `buckets.json` 에서 위 8 개를 빼면 나온다.

⚠ **litdb 는 다음 판에서 뺀다** (사용자 지시).  `buckets.json` 의 `litdb#1~4` = **73 파일**.
정본 litdb 는 별도 브랜치(`claude/friendly-meitner-lldvar`) 소관이라 이 브랜치의 사본을
감사하는 것은 의미가 적다.
⇒ 다음 판 대상 = **702 − 73 = 629 파일 (79 버킷)**.

---

## 다음 판에 넣을 "업그레이드" (지금 알고 있는 것)

1. **버킷을 더 크게** — 지금은 버킷당 평균 9 파일이라 에이전트 기동·오리엔테이션 비용이
   실제 파일 작업보다 크다.  30~40 파일씩 묶으면 같은 일을 **1/3 의 에이전트**로 한다.
   (실측: 버킷 하나당 벽시계 ~5 분, 그 중 상당 부분이 리포 파악이다.)
2. **`scripts/` 를 우선순위 맨 앞으로** — 487 파일로 가장 크고, 1 차 감사 커버리지가
   **1.6 %** 였다.  리포의 계산 기계 전부가 여기 있다.
3. **데이터 버킷은 스키마만** — `docs#1/#2` 처럼 전부 `data` 로 끝나는 버킷은 파일마다
   서술할 것이 적다.  헤더·행 수·참조자만 뽑는 가벼운 스키마로 분리.
4. **인벤토리 계약은 그대로 둔다** — 8/8 버킷이 배정 파일을 100 % 돌려줬다.  이것이
   커버리지를 *주장이 아니라 계산*으로 만드는 유일한 장치다 (CL-89).
5. 스크립트는 `/tmp/.../scratchpad/gap_audit_r2.js` 에 있고 **버킷을 파일 안에 박아
   넣는다** — args 전달과 에이전트 키 복구는 **각각 한 번씩 조용히 실패했다** (args 가
   문자열로 도착해 0 에이전트로 25 ms 만에 "완료" · 키 복구 에이전트가 산문 한 문장을
   버킷 키로 반환).  이 설계는 유지할 것.
   ⚠ 스크래치패드는 세션 소멸과 함께 사라진다 — 다음 주에는 `buckets.json` 에서 다시 만든다.

---

## 커버리지 — **계산으로 증명했다** (2026-09-09, 주장 아님)

`docs/data/audit_20260909/coverage.json` · 남은 목록 `uncovered.json`.
전부 **에이전트가 실제로 inventory 에 돌려준 경로**만 세었다 (glob 이 덮는다고 한 범위가 아니다).

```
git 추적                                          1,894
  1차 감사 (23 영역)                                710   (엄격 510 + 디렉터리 항목 전개 291)
  2차 갭 감사 (8/94 버킷, 보류)                       84
  3차 잔여 감사 (21/21 버킷)                         536   ← assigned 536 · never_opened 0
  ─────────────────────────────────────────────────────
  합집합                                          1,283   = 67.7 %
  남은 것                                           611
```

남은 611 의 분포 — **다음 주 갭 감사가 정확히 이것을 덮는다**:

| 개수 | 자리 |
|---|---|
| **408** | `scripts/` ← 리포의 계산 기계.  최우선 |
| 72 | `litdb/` ← ⚠ 사용자 지시로 **제외** |
| 44 | `이종기술/` |
| 40 | `se_curve/` |
| 16 | `machine-learning/` |
| 31 | `.claude` 9 · `docs` 8 · `heckel` 4 · `webapp` 4 · 루트 2 · `skills` 2 · `.github` 1 · `pipeline` 1 |

⇒ litdb 를 빼면 다음 주 대상은 **539 파일**이고, 끝나면 커버리지는 **96.2 %** 가 된다
(1,283 + 539 = 1,822 / 1,894).  나머지 72 는 정본이 다른 브랜치인 litdb 다.
⚠ 그때까지 *"전수"* 라고 쓰지 않는다 — 그것이 CL-89 의 요점이다.
