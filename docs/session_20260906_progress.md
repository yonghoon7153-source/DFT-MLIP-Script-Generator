# 세션 진행 (2026-09-04 ~ 09-06) — 압축 전 대피소

원장에 이미 들어간 것은 **CL-66 ~ CL-69** (`docs/reviews/claims.json`).
이 파일은 **원장에 없는 진행 상태**만 담는다.

---

## 1. 원고 — ✅ 닫힘

| | |
|---|---|
| Table S2 | PTFE 기하 2행 **삭제** · ν(DEM contact) `Calibrated → Assumed` · 각주 ② 신설 |
| 각주 ② | *"PTFE was seeded as drawn fibrils: each fibril conserves its own volume V while being drawn to a lognormally distributed length L, so that its diameter follows d ∝ √(V/L)."* |
| SI 참고문헌 | `[S6]`·`[S7]` **삭제** — 목록은 `[S5]` 에서 끝난다 |
| Table S3 · Figure 4/S16 캡션 | 09-04 확정분 그대로 |

**모델 입력은 한 글자도 안 바뀌었다** ⇒ 재시뮬 없음, 보고값 불변.
상세 = `docs/reviews/ms_readthrough_20260903.md` 말미.

⚠ **남은 확인 3건**: Figure 4b 가 `54.0 / 70.6` 인지 · Methods 의 `reconstructed → constructed` ·
`[S6]` 삭제 후 본문에서 그 번호를 참조하는 곳이 없는지.

---

## 2. 로컬 LHS 30건 — ✅ 완주, 판정은 CL-69

- **30/30 완주 · 실패 0** (WSL `~/lhs_local`, 09-05 04:39).
- 퍼콜 추출 **30/30 JSON** (`~/lhs_local/perc/*.json`).
- **28/28 percolate** (107·110 은 계기 결함 = CL-69) ⇒ 이 배치는 **상한 `φ_c < 0.493`** 만 준다.
- φ_c 는 **`lhsx_` 확장 배치**가 낸다 (φ_AM 0.152–0.486, 0.33 미만 38건).

### 디스크 정리 (원장에 없음)
```
74 GB → 21 GB   (53 GB 회수)   남긴 파일 106개
S_A(마지막 contact) ≠ S_B(최대 atom) 가 30개 중 8개 — 전부 5,000 스텝 차
  103 · 104 · 109 · 112 · 113 · 114 · 123 · 125
```
★ **합집합 규칙이 값을 했다** — 한쪽 규약만 남겼으면 그 8개에서 다른 분석기가 조용히 다른
파일을 읽었다.  특히 `lhs00_104` 는 `atom_2425000`(= S_B)로 추출했으므로 규약 A 만 남겼으면
**30/30 추출이 무효**가 될 뻔했다.
⚠ 남은 21 GB 는 사실상 전부 `restart_*` — 삭제 판단 미결.

---

## 3. OAT — ✅ 판정, 정본은 CL-68

`~/oat.csv` (19행).  요지: `∂ε/∂μ_pp ≠ 0` (잡음의 36배) 이지만
**마찰 범위 전체 1.89 %p vs 연화 28.7 %p = 15.2배** ⇒ 마찰은 연화의 대체재가 아니다.
`cor` 가 잡음의 0.03배로 나와 **계기가 0 을 낼 수 있음**을 증명한다.

---

## 4. kgy 3×3 closure 스윕 — ⏳ **런 완주, 판정 대기**

```
9/9 격자점 × 16팔 = 144팔 전부 완주   (2026-09-06 16:11:11 KST)
경과 2일 21시간 (09-03 13:56 시작)
축: C₁ = σ_AM_S/σ_VGCF  (as 3.3333e-4 · 0.01 · 0.3)
    C₂ = σ_SDCP/σ_VGCF  (sd 2.5 · 250 · 25000)
    regime = centerline (본문 규약) · 각 격자점 8 origin × 2 침대
결과 = **봉인 상태** (파이프라인이 비를 일부러 안 찍는다)
```

### 판정 규칙 — 두 개를 섞지 말 것

**적용할 것** = `docs/reviews/sigma_closure_sweep_prereg_20260902.md` §4 (δ = 0.01):

| 결과 | 판정 | 원고 영향 |
|---|---|---|
| 전 격자점 `R̄ − 3·SD > 1.01` | **DIRECTION-ROBUST** | 방향 주장 유지 |
| 전부 `R̄ > 1` 이나 일부 미달 | **WEAK** | *"넘지만 크기가 closure 의존"* 으로 문장 수정 |
| 일부 `R̄ < 1` | **SIGN-UNRESOLVED** | ⚠ 방향 주장 금지 · 부호 지도를 SI 에 |

**품질 게이트만 가져올 것** = `sdcp_gain_prereg_v2_20260816.md` §5-1·5-2
(`cg_info ≠ 0` 있으면 보류 · 8팔 SE > 1.17 %p 면 보류).
⛔ **v2 §5-3~5 의 `1.05 / 1.025` 문턱은 적용하지 않는다** — 그것은 *"SDCP 이득이 실재하는가"*
를 묻는 다른 질문의 문턱이다.

⚠ **보고에 격자점이 9 였음을 명시**한다 (등록은 25, §3-2 가 축소를 허용하되 문서를 고치지 말고
결과에 명시하라고 적었다).
⛔ **origin SD 와 격자점 사이 폭을 합치지 않는다** (§3-3) — 앞은 한 침대의 완전 factorial 이라
표준오차가 아니고, 뒤는 시나리오 범위지 불확실성 밴드가 아니다.

### 판정 실행 (미완)
`--scan ~/sdcp` 가 payload 130.9 MB × 144 ≈ **19 GB** 를 훑어 사실상 멈춘다.
⇒ 9격자점만 심볼릭 링크로 모은 `~/sdcp_closure9` 를 스캔하고, `nohup … &` 로 띄운다.

---

## 5. ibb 클러스터 — ⏳ 진행 중

```
09-05 14:41 기준
lhs00_   총 109  완주 77  실행 26  대기 6   실패 0
lhsx_    총  64  완주 19  실행 34  대기 0   실패 11   ← ⚠
```
- **5일 벽시계 한도**, 재시작이 시계를 리셋한다 (`경과 + 남음 = 5-00:00`).
- ⚠ **`lhsx_` 실패 11건** — 09-04→09-05 사이에 발생, 완주는 **0 증가**.
  φ_c 를 내는 배치가 그쪽이므로 **재시작 우선순위는 `lhsx_`**.
- ⚠ **진행률을 스텝/경과로 읽지 말 것** — `_r1` 은 restart 라 스텝은 누적, 경과는 이번 런이다
  (035_r1 이 1,370 step/s 로 보였던 것은 그 착시).  압력 단계로 볼 것.
- 압력 0.30 도달은 대략 3.5–4M 스텝.  그 기준 남은 시간(⚠ 자릿수만):
  `030_r1 ≈ 12일 · 034_r1 ≈ 12일 · 004_r1 ≈ 21일 · 029_r1 ≈ 31일`.
- ★ 재시작 **전에** `lhs_watch.sh` 출력을 저장해 두면 `_r2` 가 5일을 얼마나 샀는지 비교된다.

---

## 6. 이번 세션에 잡힌 도구 결함 (미수정)

| # | 결함 | 상태 |
|---|---|---|
| a | `after_sweep.sh` 가 `wait <비-자식 PID>` 로 **즉시 죽었다** — 3일간 대기하는 척했다 | 미수정.  대기 루프는 **주기적으로 한 줄씩 찍게** 할 것 |
| b | `tmux new -d -s afterq` 가 **같은 이름이 이미 있어 조용히 실패** | 위와 같은 사건 |
| c | `check_review_findings --selftest` **19b 가 간헐 실패** (단독 15/15 통과, `check_all.sh` 안에서 5회 중 2회 실패).  CPU 부하·선행 검사기 오염 둘 다 재현 실패 | **원인 미상** |
| d | `_commit_exists` 가 **fail-open** (`except Exception: return True`) — git 이 있는데 실패한 경우와 git 이 없는 환경을 구분 못 한다 | (c) 의 원인이라는 **증거는 없다**.  별개 항목 |
| e | QE 재시작 스크립트가 `_control_structures`(= 구조 저장소, `.in` 없음)를 **런으로 오판** | 판정 조건에 "QE 입력 존재" 를 넣을 것 |

★ (a)(b) 와 LHS `done2.txt` 가 118 에서 기록을 멈춘 것은 **같은 부류** —
**감시자가 안 돌고 있어도 돌고 있는 것과 똑같이 보인다.**  신호는 내내 있었다: **로그 mtime**.

---

## 7. 내 오류 기록 (이번 세션)

1. `additives.py:38` 의 **모듈 상수를 침대 안의 값으로 오독** → CL-66 전체가 잘못된 전제 위에
   있었다.  저자 지적으로 뒤집혔다.  ⇒ 규칙: **상수를 값으로 인용하기 전에 그 상수를
   소비하는 호출부를 읽는다.**
2. `Total wall time` 을 LIGGGHTS 완주 마커로 제시 — **LAMMPS 최신판 문구**였다.
3. `Loop time of` 를 완주 마커로 제시 — **run 명령마다** 찍히는 진행 카운터였다
   (진짜 마커는 덱이 찍는 `Finished!`).
4. 덤프 경로를 `post/` 로 추측 — 실제는 `post_lhs00_1NN/`.
5. `101 ↔ 110` 을 **같은 설계점**이라 단정 — φ 가 비슷했을 뿐 `d_se` 가 다르다.
6. *"40 µm 는 상자를 80 % 가로지른다"* — **윤곽 길이**를 span 으로 착각 (실제 8 %).

⇒ 공통 원인: **확인 없이 단정.**  `grep`·`ls`·실행으로 먼저 확인한다.
